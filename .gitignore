.idea
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
backend/config.yaml

# Persönliche Claude-Code-Einstellungen (nicht versionieren)
.claude/settings.local.json

# MkDocs lokaler Build-Output
mkdocs/site/

# Lokale Compose-Laufzeitordner (nicht versionieren)
/data/
/config/
/targets/

# Local uv dev-server runtime (scripts/devserver.sh) — never versioned.
/.dev/

/mocks/*
!/mocks/.gitkeep
