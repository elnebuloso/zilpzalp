# Analyse-Kern (Meilenstein 2) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Aus einem PDF deterministisch einen brauchbaren Dateinamen-Vorschlag erzeugen — über drei isolierte Module: `extractor` (OpenDataLoader → `Document`), `analyzer` (`Document` → erkannte Fakten inkl. **aller** Datumskandidaten) und `suggestion` (Fakten + Config → finaler Name + Zielordner).

**Architecture:** OpenDataLoader (Java/JVM) liefert Inhalte **und** Struktur; der `extractor` ist der einzige Ort mit Dateisystem + JVM und normalisiert ODLs JSON in ein schlankes eigenes `Document`/`Block`-Modell. `analyzer` und `suggestion` sind reine Funktionen (`Document + Config → …`), ohne JVM/IO, mit handgebauten `Document`-Fixtures testbar. ODL erkennt **keine** Datumsangaben — das tut `analyzer` deterministisch per Regex über die Block-Texte, die Label-Zuordnung erfolgt strukturgestützt (Tabellenzelle / Inline-Kontext / zugehörige Überschrift).

**Tech Stack:** Python 3.12, `opendataloader-pdf` (benötigt Java 11+ JRE), pydantic-Config aus Meilenstein 1, pytest, uv.

**Referenz:** Design-Spec `docs/superpowers/specs/2026-06-13-1435-zilpzalp-mvp-design.md` §3, §3.1, §4.1, §4.3, §5, §7.

---

## Design-Notizen (verbindliche Entscheidungen)

Diese Entscheidungen verfeinern die Spec und gelten für alle Tasks:

- **Datentypen & ihr Fluss:**
  - `Block`/`Document` (Modul `document.py`) — Ausgabe `extractor`, Eingabe `analyzer`.
  - `DateCandidate`, `Analysis` (Modul `analyzer.py`) — Ausgabe `analyzer`, Eingabe `suggestion`.
  - `Suggestion` (Modul `suggestion.py`) — Endergebnis für die spätere Web-UI.
- **`Analysis.full_text`** trägt den kompletten Dokumenttext (für Regel-Bedingung `keywords_any`).
  Eine separate Keyword-Extraktion gibt es bewusst **nicht** (YAGNI): Die Produktanforderung
  „wiederkehrende Dokumente über Regeln stabilisieren" ist über `keywords_any` gegen `full_text`
  vollständig erfüllt; eine geratene Keyword-Liste hätte keinen deterministischen Mehrwert.
- **Regelauswertung lebt in `suggestion`** (nicht im `analyzer`). `analyzer` liefert nur rohe
  Fakten; `suggestion` matcht Config-Regeln (erste Übereinstimmung gewinnt) und wendet `apply` an.
  Unterstützte `match`-Schlüssel: `sender_contains`, `keywords_any`. Andere Schlüssel werden
  ignoriert (kein Fehler) — Config-`rules` bleiben bewusst ein offenes Schema (Meilenstein 1).
- **§4.3-Invariante:** `analyzer` sammelt **alle** Datumskandidaten, entfernt/fasst nie zusammen.
  `suggestion` darf nur **einen vorauswählen** (`preselected_date_index`); alle bleiben erhalten.
- **Beschreibung** ist deterministisch am schwersten (17.2): Default leer; nur eine greifende
  Regel füllt sie über `apply.description`. Kein geratener Beschreibungstext.
- **Datensparsamkeit (§11):** `extractor` schreibt ODL-Output in ein `tempfile.TemporaryDirectory`
  und löscht es nach dem Zurücklesen. Kein Volltext landet je auf Platte.

---

## File Structure

| Datei | Verantwortung |
|---|---|
| `backend/src/zilpzalp/document.py` | `Block`, `Document` dataclasses (reines Datenmodell) |
| `backend/src/zilpzalp/extractor.py` | ODL-Adapter: `extract(path)→Document`, JSON→`Document`-Mapping, `ExtractionError` |
| `backend/src/zilpzalp/analyzer.py` | `DateCandidate`, `Analysis`, `analyze(document, config)→Analysis`, eingebaute Datumserkennung + strukturgestützte Labels + Absender/Typ-Heuristik |
| `backend/src/zilpzalp/suggestion.py` | `Suggestion`, `suggest(analysis, config)→Suggestion`, Regel-Matching, `preferred_date`, Pattern-Rendering, Zielordner |
| `backend/tests/test_document.py` | Konstruktions-Smoke des Modells |
| `backend/tests/test_extractor.py` | Mapping (gegen echtes ODL-JSON) + Integrationslauf (JVM) + Temp-Cleanup |
| `backend/tests/test_analyzer.py` | Datumskandidaten (mehrere Formate/Arten), Labels, Absender/Typ — Hand-`Document`-Fixtures |
| `backend/tests/test_suggestion.py` | Pattern-Rendering, Regelpriorität, `preferred_date`, Zielordner |
| `backend/tests/fixtures/` | erzeugte Sample-PDFs + **eingecheckte** echte ODL-JSON-Captures |

---

## Task 1: OpenDataLoader-Abhängigkeit + Java-Verfügbarkeit

**Files:**
- Modify: `backend/pyproject.toml`

- [ ] **Step 1: Java-Laufzeit prüfen**

Run: `java -version`
Expected: Eine Version `11` oder höher wird ausgegeben (z. B. `openjdk version "17..."`).
Falls Java fehlt: lokal eine Temurin-17-JRE installieren (Design-Spec §1) — OpenDataLoader
startet pro Aufruf eine JVM und schlägt sonst zur Laufzeit fehl.

- [ ] **Step 2: Dependency eintragen**

In `backend/pyproject.toml` die `dependencies`-Liste erweitern:

```toml
dependencies = [
    "fastapi>=0.115",
    "pydantic>=2.7",
    "pyyaml>=6.0",
    "opendataloader-pdf>=2.0",
]
```

Und im `dev`-Group `pypdf` für das Erzeugen von Fixture-PDFs ergänzen:

```toml
dev = [
    "pytest>=8.2",
    "httpx>=0.27",
    "ruff>=0.5",
    "pypdf>=4.0",
]
```

- [ ] **Step 3: Synchronisieren und importierbar machen**

Run: `cd backend && uv sync`
Expected: Installation ohne Fehler; `opendataloader-pdf` und `pypdf` erscheinen im Lock.

Run: `cd backend && uv run python -c "import opendataloader_pdf; print('ok')"`
Expected: `ok`

- [ ] **Step 4: Commit**

```bash
git add backend/pyproject.toml backend/uv.lock
git commit -m "build(backend): add opendataloader-pdf and pypdf (dev)"
```

---

## Task 2: Echte ODL-JSON-Captures als Fixtures festhalten

Ziel: Bevor Mapping-Code geschrieben wird, das **reale** JSON-Schema (besonders Tabellen, und
ob die Struktur flach oder über `kids` verschachtelt ist) aus echten PDFs festhalten und
einchecken. Alle folgenden Mapping-Tests laufen gegen diese Captures.

**Files:**
- Create: `backend/tests/fixtures/make_fixtures.py`
- Create (generiert): `backend/tests/fixtures/invoice.pdf`, `backend/tests/fixtures/empty.pdf`
- Create (generiert): `backend/tests/fixtures/invoice.odl.json`
- Create: `backend/tests/fixtures/SCHEMA_NOTES.md`

- [ ] **Step 1: Fixture-Generator schreiben**

Create `backend/tests/fixtures/make_fixtures.py`:

```python
"""Erzeugt deterministische Test-PDFs. Einmalig per `uv run python` ausführen."""
from pathlib import Path

from pypdf import PdfWriter

HERE = Path(__file__).parent


def _text_pdf(path: Path, lines: list[str]) -> None:
    writer = PdfWriter()
    page = writer.add_blank_page(width=595, height=842)  # A4 in PDF-Punkten
    # pypdf bettet echten Text ein; eine Zeile pro Eintrag, von oben nach unten.
    y = 800
    for line in lines:
        page.add_text(line, x=72, y=y, font_size=11)  # type: ignore[attr-defined]
        y -= 24
    with path.open("wb") as fh:
        writer.write(fh)


def main() -> None:
    _text_pdf(
        HERE / "invoice.pdf",
        [
            "Stadtwerke Musterstadt GmbH",
            "Rechnung",
            "Rechnungsdatum: 15.01.2026",
            "Leistungsdatum: 31.12.2025",
            "Faellig am: 01.02.2026",
            "Stromabschlag Januar",
        ],
    )
    # PDF ohne jeden eingebetteten Text (simuliert reinen Scan / unlesbar):
    writer = PdfWriter()
    writer.add_blank_page(width=595, height=842)
    with (HERE / "empty.pdf").open("wb") as fh:
        writer.write(fh)


if __name__ == "__main__":
    main()
```

> Hinweis: Falls `page.add_text` in der installierten `pypdf`-Version nicht verfügbar ist,
> stattdessen `reportlab` (zu `dev` hinzufügen) verwenden, um Text sicher einzubetten. Wichtig
> ist nur: `invoice.pdf` enthält echten, extrahierbaren Text mit **mehreren** Datumsangaben,
> `empty.pdf` enthält **keinen** Text.

- [ ] **Step 2: PDFs erzeugen**

Run: `cd backend && uv run python tests/fixtures/make_fixtures.py`
Expected: `tests/fixtures/invoice.pdf` und `tests/fixtures/empty.pdf` existieren.

Verifizieren, dass `invoice.pdf` Text trägt:
Run: `cd backend && uv run python -c "from pypdf import PdfReader; print(PdfReader('tests/fixtures/invoice.pdf').pages[0].extract_text())"`
Expected: Die Zeilen inkl. `Rechnungsdatum: 15.01.2026` werden ausgegeben. (Falls leer → auf
reportlab umstellen, siehe Hinweis oben, sonst kann ODL keinen Text finden.)

- [ ] **Step 3: Echtes ODL-JSON erzeugen und einchecken**

Run:
```bash
cd backend && uv run python -c "
import opendataloader_pdf, tempfile, pathlib, shutil
with tempfile.TemporaryDirectory() as d:
    opendataloader_pdf.convert(input_path=['tests/fixtures/invoice.pdf'], output_dir=d, format='json')
    out = list(pathlib.Path(d).glob('*.json'))
    print('OUTPUT FILES:', [p.name for p in out])
    shutil.copy(out[0], 'tests/fixtures/invoice.odl.json')
"
```
Expected: Mindestens eine `.json`-Datei wird erzeugt und nach `tests/fixtures/invoice.odl.json`
kopiert. Den ausgegebenen Dateinamen (`OUTPUT FILES: …`) notieren — er zeigt das Namensschema,
das `extract()` in Task 6 per Glob findet.

- [ ] **Step 4: Schema dokumentieren**

`backend/tests/fixtures/invoice.odl.json` öffnen und in `backend/tests/fixtures/SCHEMA_NOTES.md`
festhalten:
- Ist die Top-Level-Struktur eine **flache Liste** von Elementen, oder ein **Baum** mit
  Kind-Listen (Schlüssel z. B. `kids`/`children`/`content`)? Den exakten Schlüsselnamen notieren.
- Die exakten Feldnamen für Text (`content`?), Typ (`type`?), Seite (`page number`?), Box
  (`bounding box`?), Überschriftsebene (`heading level`?).
- Wie **Tabellen** dargestellt werden: Zeilen/Zellen — exakte Schlüssel und Verschachtelung.

> Diese Notizen sind die Quelle der Wahrheit für die Mapping-Konstanten in Task 4 & 5. Weichen
> die echten Feldnamen von den unten verwendeten ab, in Task 4/5 die Konstanten anpassen — die
> Tests laufen ohnehin gegen das echte Capture.

- [ ] **Step 5: Commit**

```bash
git add backend/tests/fixtures/
git commit -m "test(extractor): capture real OpenDataLoader JSON fixtures"
```

---

## Task 3: Dokumentmodell (`Document` / `Block`)

**Files:**
- Create: `backend/src/zilpzalp/document.py`
- Test: `backend/tests/test_document.py`

- [ ] **Step 1: Failing test schreiben**

Create `backend/tests/test_document.py`:

```python
from zilpzalp.document import Block, Document


def test_block_and_document_construct():
    heading = Block(kind="heading", text="Rechnung", page=1, bbox=(72, 800, 520, 820), level=1)
    table = Block(
        kind="table",
        text="Rechnungsdatum 15.01.2026",
        page=1,
        bbox=(72, 600, 520, 700),
        cells=[["Feld", "Wert"], ["Rechnungsdatum", "15.01.2026"]],
    )
    doc = Document(blocks=[heading, table])

    assert doc.blocks[0].level == 1
    assert doc.blocks[1].cells[1][1] == "15.01.2026"
    assert doc.blocks[0].cells is None
```

- [ ] **Step 2: Test laufen lassen (muss fehlschlagen)**

Run: `cd backend && uv run pytest tests/test_document.py -v`
Expected: FAIL mit `ModuleNotFoundError: No module named 'zilpzalp.document'`

- [ ] **Step 3: Modell implementieren**

Create `backend/src/zilpzalp/document.py`:

```python
from __future__ import annotations

from dataclasses import dataclass
from typing import Literal

BlockKind = Literal["heading", "paragraph", "table", "list", "caption"]


@dataclass(frozen=True)
class Block:
    kind: BlockKind
    text: str                                   # Plaintext des Blocks
    page: int
    bbox: tuple[float, float, float, float]      # (links, unten, rechts, oben), PDF-Punkte
    level: int | None = None                     # nur bei heading: Hierarchieebene
    cells: list[list[str]] | None = None         # nur bei table: Zeilen × Spalten


@dataclass(frozen=True)
class Document:
    blocks: list[Block]                          # in korrigierter Lesereihenfolge
```

- [ ] **Step 4: Test laufen lassen (muss bestehen)**

Run: `cd backend && uv run pytest tests/test_document.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/src/zilpzalp/document.py backend/tests/test_document.py
git commit -m "feat(document): add Block/Document model"
```

---

## Task 4: ODL-JSON → Document — einfache Elemente

Mapping für `heading`/`paragraph`/`list`/`caption` über einen rekursiven Walk (robust gegen
flache **oder** verschachtelte ODL-Struktur). Tabellen kommen in Task 5 dazu.

**Files:**
- Modify: `backend/src/zilpzalp/extractor.py` (neu)
- Test: `backend/tests/test_extractor.py` (neu)

- [ ] **Step 1: Failing test gegen das echte Capture schreiben**

Create `backend/tests/test_extractor.py`:

```python
import json
from pathlib import Path

from zilpzalp.extractor import document_from_odl

FIXTURES = Path(__file__).parent / "fixtures"


def test_document_from_odl_simple_elements():
    data = json.loads((FIXTURES / "invoice.odl.json").read_text(encoding="utf-8"))
    doc = document_from_odl(data)

    # Alle Block-Texte zusammengezogen müssen die eingebetteten Zeilen enthalten.
    full = "\n".join(b.text for b in doc.blocks)
    assert "Rechnungsdatum: 15.01.2026" in full
    assert "Leistungsdatum: 31.12.2025" in full
    assert "Stadtwerke" in full
    # Mindestens eine Überschrift ("Rechnung") wurde als heading erkannt.
    assert any(b.kind == "heading" for b in doc.blocks)
    # Blocks tragen 1-indizierte Seitenzahlen.
    assert all(b.page >= 1 for b in doc.blocks)
```

- [ ] **Step 2: Test laufen lassen (muss fehlschlagen)**

Run: `cd backend && uv run pytest tests/test_extractor.py -v`
Expected: FAIL mit `ImportError`/`ModuleNotFoundError` für `zilpzalp.extractor`.

- [ ] **Step 3: Mapping implementieren**

Create `backend/src/zilpzalp/extractor.py`:

```python
from __future__ import annotations

from typing import Any

from zilpzalp.document import Block, BlockKind, Document

# --- ODL-JSON-Schlüssel (gegen tests/fixtures/SCHEMA_NOTES.md prüfen/anpassen) ---
_KEY_TYPE = "type"
_KEY_TEXT = "content"
_KEY_PAGE = "page number"
_KEY_BBOX = "bounding box"
_KEY_HEADING_LEVEL = "heading level"
_KEY_CHILDREN = "kids"  # Falls SCHEMA_NOTES "children"/anderen Namen zeigt: hier anpassen.

# ODL-Typ → unser BlockKind. Unbekannte Typen werden übersprungen.
_KIND_MAP: dict[str, BlockKind] = {
    "heading": "heading",
    "title": "heading",
    "paragraph": "paragraph",
    "text": "paragraph",
    "list": "list",
    "caption": "caption",
    "table": "table",
}


def _bbox(node: dict[str, Any]) -> tuple[float, float, float, float]:
    raw = node.get(_KEY_BBOX) or [0.0, 0.0, 0.0, 0.0]
    left, bottom, right, top = (float(v) for v in raw[:4])
    return (left, bottom, right, top)


def _simple_block(node: dict[str, Any], kind: BlockKind) -> Block | None:
    text = (node.get(_KEY_TEXT) or "").strip()
    if not text:
        return None
    level = node.get(_KEY_HEADING_LEVEL) if kind == "heading" else None
    return Block(
        kind=kind,
        text=text,
        page=int(node.get(_KEY_PAGE, 1)),
        bbox=_bbox(node),
        level=int(level) if level is not None else None,
    )


def _walk(node: Any, blocks: list[Block]) -> None:
    if isinstance(node, list):
        for child in node:
            _walk(child, blocks)
        return
    if not isinstance(node, dict):
        return
    kind = _KIND_MAP.get(str(node.get(_KEY_TYPE, "")).lower())
    if kind == "table":
        return  # Tabellen behandelt Task 5; hier (noch) überspringen, NICHT hineinlaufen.
    if kind is not None:
        block = _simple_block(node, kind)
        if block is not None:
            blocks.append(block)
    _walk(node.get(_KEY_CHILDREN, []), blocks)


def document_from_odl(data: Any) -> Document:
    blocks: list[Block] = []
    _walk(data, blocks)
    return Document(blocks=blocks)
```

- [ ] **Step 4: Test laufen lassen (muss bestehen)**

Run: `cd backend && uv run pytest tests/test_extractor.py -v`
Expected: PASS. Falls FAIL wegen abweichender Feldnamen: die `_KEY_*`/`_KIND_MAP`-Konstanten
gemäß `SCHEMA_NOTES.md` korrigieren und erneut laufen lassen.

- [ ] **Step 5: Commit**

```bash
git add backend/src/zilpzalp/extractor.py backend/tests/test_extractor.py
git commit -m "feat(extractor): map ODL JSON to Document (simple elements)"
```

---

## Task 5: ODL-JSON → Document — Tabellen (Zellen)

**Files:**
- Modify: `backend/src/zilpzalp/extractor.py`
- Test: `backend/tests/test_extractor.py`

- [ ] **Step 1: Failing test gegen Tabellen-Capture schreiben**

> Voraussetzung: `SCHEMA_NOTES.md` (Task 2) beschreibt die echte Tabellenstruktur. Falls
> `invoice.pdf` keine echte Tabelle erzeugt hat, in `make_fixtures.py` eine Zeile als zwei
> Spalten („Rechnungsdatum" | „15.01.2026") anlegen, Fixtures + Capture neu erzeugen und
> committen, sodass das Capture mindestens eine Tabelle enthält.

In `backend/tests/test_extractor.py` ergänzen:

```python
def test_document_from_odl_table_cells_when_present():
    data = json.loads((FIXTURES / "invoice.odl.json").read_text(encoding="utf-8"))
    doc = document_from_odl(data)

    tables = [b for b in doc.blocks if b.kind == "table"]
    if not tables:  # Fixture enthält (noch) keine Tabelle -> Test ist dann nicht aussagekräftig.
        import pytest

        pytest.skip("Fixture enthaelt keine Tabelle; siehe make_fixtures.py")

    table = tables[0]
    assert table.cells is not None
    # Jede Zeile ist eine Liste von Zell-Strings; mindestens eine Zelle traegt ein Datum.
    flat = [cell for row in table.cells for cell in row]
    assert any("15.01.2026" in cell for cell in flat)
    # Der Block-Text enthaelt die Zell-Inhalte (fuer die Volltextsuche spaeter).
    assert "15.01.2026" in table.text
```

- [ ] **Step 2: Test laufen lassen (muss fehlschlagen oder skippen)**

Run: `cd backend && uv run pytest tests/test_extractor.py::test_document_from_odl_table_cells_when_present -v`
Expected: FAIL (Tabellen werden in Task 4 noch übersprungen) — oder SKIP, falls noch keine
Tabelle im Fixture. Bei SKIP zuerst Fixture mit Tabelle erzeugen (siehe Voraussetzung), dann FAIL.

- [ ] **Step 3: Tabellen-Mapping implementieren**

In `backend/src/zilpzalp/extractor.py` die Schlüssel für Tabellen ergänzen (Namen gemäß
`SCHEMA_NOTES.md` anpassen) und den `table`-Zweig in `_walk` füllen:

```python
# zu den _KEY_* Konstanten ergaenzen (Namen aus SCHEMA_NOTES.md):
_KEY_ROWS = "rows"        # Liste der Tabellenzeilen
_KEY_CELLS = "cells"      # Liste der Zellen je Zeile
```

```python
def _table_cells(node: dict[str, Any]) -> list[list[str]]:
    rows: list[list[str]] = []
    for row in node.get(_KEY_ROWS, []) or []:
        cells = []
        for cell in row.get(_KEY_CELLS, []) or []:
            # Eine Zelle kann selbst verschachtelte Text-Knoten haben -> einsammeln.
            parts: list[Block] = []
            _walk(cell, parts)
            inline = (cell.get(_KEY_TEXT) or "").strip()
            text = inline or " ".join(p.text for p in parts).strip()
            cells.append(text)
        if cells:
            rows.append(cells)
    return rows


def _table_block(node: dict[str, Any]) -> Block | None:
    cells = _table_cells(node)
    if not cells:
        return None
    text = "\n".join(" ".join(row) for row in cells)
    return Block(kind="table", text=text, page=int(node.get(_KEY_PAGE, 1)), bbox=_bbox(node), cells=cells)
```

Und in `_walk` den `table`-Zweig ersetzen:

```python
    if kind == "table":
        block = _table_block(node)
        if block is not None:
            blocks.append(block)
        return  # nicht zusätzlich in die Zellen hineinlaufen
```

- [ ] **Step 4: Test laufen lassen (muss bestehen)**

Run: `cd backend && uv run pytest tests/test_extractor.py -v`
Expected: PASS (beide Mapping-Tests). Bei FAIL: Tabellen-Schlüssel gemäß `SCHEMA_NOTES.md`
korrigieren.

- [ ] **Step 5: Commit**

```bash
git add backend/src/zilpzalp/extractor.py backend/tests/test_extractor.py
git commit -m "feat(extractor): map ODL tables to Block.cells"
```

---

## Task 6: `extract()` — JVM-Aufruf, Temp-Cleanup, Fehlerpfad

**Files:**
- Modify: `backend/src/zilpzalp/extractor.py`
- Test: `backend/tests/test_extractor.py`

- [ ] **Step 1: Failing tests schreiben (Integration + Fehlerpfad)**

In `backend/tests/test_extractor.py` ergänzen:

```python
import pytest

from zilpzalp.extractor import ExtractionError, extract


@pytest.mark.integration
def test_extract_real_pdf_returns_document():
    doc = extract(FIXTURES / "invoice.pdf")
    full = "\n".join(b.text for b in doc.blocks)
    assert "15.01.2026" in full


@pytest.mark.integration
def test_extract_pdf_without_text_raises():
    with pytest.raises(ExtractionError):
        extract(FIXTURES / "empty.pdf")
```

`integration` als Marker registrieren — in `backend/pyproject.toml` unter `[tool.pytest.ini_options]`:

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: benoetigt eine echte JVM (OpenDataLoader); langsam",
]
```

- [ ] **Step 2: Tests laufen lassen (müssen fehlschlagen)**

Run: `cd backend && uv run pytest tests/test_extractor.py -k extract_real -v`
Expected: FAIL mit `ImportError` für `extract`/`ExtractionError`.

- [ ] **Step 3: `extract()` implementieren**

In `backend/src/zilpzalp/extractor.py` oben ergänzen:

```python
import json
import tempfile
from pathlib import Path

import opendataloader_pdf
```

Und am Ende der Datei:

```python
class ExtractionError(Exception):
    """PDF lieferte keinen verwertbaren Text (korrupt, leer oder reiner Scan)."""


def extract(pdf_path: str | Path) -> Document:
    pdf_path = Path(pdf_path)
    with tempfile.TemporaryDirectory() as tmp:
        opendataloader_pdf.convert(
            input_path=[str(pdf_path)],
            output_dir=tmp,
            format="json",
        )
        outputs = list(Path(tmp).glob("*.json"))
        if not outputs:
            raise ExtractionError(f"OpenDataLoader erzeugte keine Ausgabe fuer {pdf_path.name!r}")
        data = json.loads(outputs[0].read_text(encoding="utf-8"))
    # TemporaryDirectory ist hier bereits geloescht -> kein Volltext bleibt auf Platte.
    document = document_from_odl(data)
    if not any(block.text.strip() for block in document.blocks):
        raise ExtractionError(f"Kein Text im PDF {pdf_path.name!r} gefunden")
    return document
```

> Falls die Notiz aus Task 2 zeigte, dass ODL die JSON in einem Unterordner statt direkt in
> `output_dir` ablegt, das Glob auf `Path(tmp).rglob("*.json")` umstellen.

- [ ] **Step 4: Tests laufen lassen (müssen bestehen)**

Run: `cd backend && uv run pytest tests/test_extractor.py -v`
Expected: PASS (alle, inkl. der `integration`-Tests). Benötigt installiertes Java (Task 1).

- [ ] **Step 5: Commit**

```bash
git add backend/src/zilpzalp/extractor.py backend/tests/test_extractor.py backend/pyproject.toml
git commit -m "feat(extractor): extract() with JVM call, temp cleanup, no-text error"
```

---

## Task 7: Analyzer — eingebaute numerische Datumsformate

`DateCandidate`, `Analysis` und die Erkennung von `TT.MM.JJJJ`/`TT.MM.JJ` und `JJJJ-MM-TT`,
inklusive Normalisierung auf `config.date_format` und Validierung (unmögliche Daten verworfen).

**Files:**
- Create: `backend/src/zilpzalp/analyzer.py`
- Test: `backend/tests/test_analyzer.py`

- [ ] **Step 1: Failing test schreiben**

Create `backend/tests/test_analyzer.py`:

```python
from pathlib import Path

from zilpzalp.config import load_config
from zilpzalp.document import Block, Document
from zilpzalp.analyzer import analyze


def _config(tmp_path: Path):
    # Minimal-Config mit existierenden Pflichtpfaden; original_handling=keep -> kein processed.
    (tmp_path / "inbox").mkdir()
    (tmp_path / "error").mkdir()
    cfg = tmp_path / "config.yaml"
    cfg.write_text(
        f"""
paths:
  watchfolder: {tmp_path / "inbox"}
  error_folder: {tmp_path / "error"}
original_handling: keep
summary_mode: never
default_pattern: "{{date}}__{{sender}}_{{doctype}}_{{description}}"
date_format: "%Y-%m-%d"
""",
        encoding="utf-8",
    )
    return load_config(cfg)


def test_numeric_dates_are_collected_and_normalized(tmp_path):
    doc = Document(blocks=[
        Block(kind="paragraph", text="Rechnungsdatum: 15.01.2026", page=1, bbox=(0, 0, 0, 0)),
        Block(kind="paragraph", text="Erstellt am 2025-12-31", page=1, bbox=(0, 0, 0, 0)),
        Block(kind="paragraph", text="Unsinn 32.13.2020 ignorieren", page=1, bbox=(0, 0, 0, 0)),
    ])
    analysis = analyze(doc, _config(tmp_path))
    normalized = [c.normalized for c in analysis.date_candidates]

    assert "2026-01-15" in normalized
    assert "2025-12-31" in normalized
    assert "2020-13-32" not in normalized            # ungueltiges Datum verworfen
    assert all(c.raw for c in analysis.date_candidates)  # roher Treffer-Text erhalten
```

- [ ] **Step 2: Test laufen lassen (muss fehlschlagen)**

Run: `cd backend && uv run pytest tests/test_analyzer.py -v`
Expected: FAIL mit `ModuleNotFoundError: No module named 'zilpzalp.analyzer'`

- [ ] **Step 3: Analyzer-Grundgerüst + numerische Daten implementieren**

Create `backend/src/zilpzalp/analyzer.py`:

```python
from __future__ import annotations

import datetime
import re
from dataclasses import dataclass, field

from zilpzalp.config import Config
from zilpzalp.document import Block, Document


@dataclass(frozen=True)
class DateCandidate:
    normalized: str            # date_format-konform (z. B. 2026-01-15)
    raw: str                   # roher Treffer-Text aus dem PDF
    label: str | None = None   # strukturgestuetzter Kontext (z. B. "Rechnungsdatum")


@dataclass(frozen=True)
class Analysis:
    date_candidates: list[DateCandidate] = field(default_factory=list)
    sender: str | None = None
    doctype: str | None = None
    description: str | None = None
    full_text: str = ""


# --- Eingebaute Datumsformate (laufen immer, ohne Config) ---
_NUMERIC = re.compile(r"\b(\d{1,2})\.(\d{1,2})\.(\d{2,4})\b")
_ISO = re.compile(r"\b(\d{4})-(\d{1,2})-(\d{1,2})\b")


def _two_digit_year(value: int) -> int:
    return 2000 + value if value <= 69 else 1900 + value


def _valid_date(year: int, month: int, day: int) -> datetime.date | None:
    try:
        return datetime.date(year, month, day)
    except ValueError:
        return None


def _numeric_matches(text: str) -> list[tuple[int, int, str, datetime.date]]:
    out: list[tuple[int, int, str, datetime.date]] = []
    for m in _NUMERIC.finditer(text):
        day, month, year = int(m.group(1)), int(m.group(2)), int(m.group(3))
        if year < 100:
            year = _two_digit_year(year)
        d = _valid_date(year, month, day)
        if d is not None:
            out.append((m.start(), m.end(), m.group(0), d))
    for m in _ISO.finditer(text):
        d = _valid_date(int(m.group(1)), int(m.group(2)), int(m.group(3)))
        if d is not None:
            out.append((m.start(), m.end(), m.group(0), d))
    return out


def _find_dates_in_text(text: str) -> list[tuple[int, int, str, datetime.date]]:
    """Alle eingebauten Formate; ueberlappende Treffer werden dedupliziert (erster gewinnt)."""
    raw = sorted(_numeric_matches(text), key=lambda t: (t[0], -(t[1] - t[0])))
    kept: list[tuple[int, int, str, datetime.date]] = []
    for start, end, hit, d in raw:
        if any(start < k_end and end > k_start for k_start, k_end, _, _ in kept):
            continue
        kept.append((start, end, hit, d))
    return kept


def analyze(document: Document, config: Config) -> Analysis:
    full_text = "\n".join(b.text for b in document.blocks)
    candidates: list[DateCandidate] = []
    for block in document.blocks:
        for _start, _end, hit, d in _find_dates_in_text(block.text):
            candidates.append(
                DateCandidate(normalized=d.strftime(config.date_format), raw=hit, label=None)
            )
    return Analysis(date_candidates=candidates, full_text=full_text)
```

- [ ] **Step 4: Test laufen lassen (muss bestehen)**

Run: `cd backend && uv run pytest tests/test_analyzer.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/src/zilpzalp/analyzer.py backend/tests/test_analyzer.py
git commit -m "feat(analyzer): collect & normalize numeric/ISO date candidates"
```

---

## Task 8: Analyzer — deutsche Langform-Daten + zweistellige Jahre

**Files:**
- Modify: `backend/src/zilpzalp/analyzer.py`
- Test: `backend/tests/test_analyzer.py`

- [ ] **Step 1: Failing test schreiben**

In `backend/tests/test_analyzer.py` ergänzen:

```python
def test_long_form_german_and_two_digit_year(tmp_path):
    doc = Document(blocks=[
        Block(kind="paragraph", text="Berlin, 5. Maerz 2026", page=1, bbox=(0, 0, 0, 0)),
        Block(kind="paragraph", text="Vertragsbeginn 01.07.99", page=1, bbox=(0, 0, 0, 0)),
    ])
    analysis = analyze(doc, _config(tmp_path))
    normalized = [c.normalized for c in analysis.date_candidates]

    assert "2026-03-05" in normalized            # "5. Maerz 2026"
    assert "1999-07-01" in normalized            # 99 -> 1999 (Pivot 69)
```

- [ ] **Step 2: Test laufen lassen (muss fehlschlagen)**

Run: `cd backend && uv run pytest tests/test_analyzer.py::test_long_form_german_and_two_digit_year -v`
Expected: FAIL (Langform `5. Maerz 2026` wird noch nicht erkannt).

- [ ] **Step 3: Langform-Erkennung ergänzen**

In `backend/src/zilpzalp/analyzer.py` nach `_ISO` ergänzen:

```python
_MONTHS_DE = {
    "januar": 1, "februar": 2, "maerz": 3, "märz": 3, "april": 4, "mai": 5,
    "juni": 6, "juli": 7, "august": 8, "september": 9, "oktober": 10,
    "november": 11, "dezember": 12,
}
_LONG_DE = re.compile(r"\b(\d{1,2})\.?\s+([A-Za-zäöüÄÖÜ]+)\s+(\d{4})\b")
```

In `_numeric_matches` vor dem `return` ergänzen (Funktion bleibt sonst gleich):

```python
    for m in _LONG_DE.finditer(text):
        month = _MONTHS_DE.get(m.group(2).lower())
        if month is None:
            continue
        d = _valid_date(int(m.group(3)), month, int(m.group(1)))
        if d is not None:
            out.append((m.start(), m.end(), m.group(0), d))
```

- [ ] **Step 4: Tests laufen lassen (müssen bestehen)**

Run: `cd backend && uv run pytest tests/test_analyzer.py -v`
Expected: PASS (alle Analyzer-Tests).

- [ ] **Step 5: Commit**

```bash
git add backend/src/zilpzalp/analyzer.py backend/tests/test_analyzer.py
git commit -m "feat(analyzer): recognize German long-form dates and 2-digit years"
```

---

## Task 9: Analyzer — optionale `date_patterns` aus der Config (additiv)

**Files:**
- Modify: `backend/src/zilpzalp/analyzer.py`
- Test: `backend/tests/test_analyzer.py`

- [ ] **Step 1: Failing test schreiben**

In `backend/tests/test_analyzer.py` ergänzen:

```python
def test_config_date_patterns_add_labeled_candidates(tmp_path):
    (tmp_path / "inbox").mkdir(exist_ok=True)
    (tmp_path / "error").mkdir(exist_ok=True)
    cfg_file = tmp_path / "config.yaml"
    cfg_file.write_text(
        f"""
paths:
  watchfolder: {tmp_path / "inbox"}
  error_folder: {tmp_path / "error"}
original_handling: keep
summary_mode: never
default_pattern: "{{date}}__{{sender}}_{{doctype}}_{{description}}"
date_format: "%Y-%m-%d"
date_patterns:
  - label: leistungsdatum
    regex: 'Leistungszeitraum bis (\\d{{2}}\\.\\d{{2}}\\.\\d{{4}})'
""",
        encoding="utf-8",
    )
    from zilpzalp.config import load_config

    doc = Document(blocks=[
        Block(kind="paragraph", text="Leistungszeitraum bis 31.12.2025", page=1, bbox=(0, 0, 0, 0)),
    ])
    analysis = analyze(doc, load_config(cfg_file))

    leistung = [c for c in analysis.date_candidates if c.label == "leistungsdatum"]
    assert len(leistung) == 1
    assert leistung[0].normalized == "2025-12-31"
```

- [ ] **Step 2: Test laufen lassen (muss fehlschlagen)**

Run: `cd backend && uv run pytest tests/test_analyzer.py::test_config_date_patterns_add_labeled_candidates -v`
Expected: FAIL (Config-`date_patterns` werden noch nicht ausgewertet; das `leistungsdatum`-Label fehlt).

- [ ] **Step 3: `date_patterns` additiv auswerten**

In `backend/src/zilpzalp/analyzer.py` in `analyze` die Block-Schleife erweitern, sodass nach den
eingebauten Treffern auch die Config-Pattern laufen. `analyze` wie folgt anpassen:

```python
def analyze(document: Document, config: Config) -> Analysis:
    full_text = "\n".join(b.text for b in document.blocks)
    candidates: list[DateCandidate] = []
    for block in document.blocks:
        for _start, _end, hit, d in _find_dates_in_text(block.text):
            candidates.append(
                DateCandidate(normalized=d.strftime(config.date_format), raw=hit, label=None)
            )
        for dp in config.date_patterns:
            for m in re.finditer(dp.regex, block.text):
                value = m.group(1) if m.groups() else m.group(0)
                parsed = _find_dates_in_text(value)
                if parsed:
                    _s, _e, hit, d = parsed[0]
                    candidates.append(
                        DateCandidate(
                            normalized=d.strftime(config.date_format), raw=value, label=dp.label
                        )
                    )
    return Analysis(date_candidates=candidates, full_text=full_text)
```

> Hinweis: Die Config-Pattern **ergänzen** (additiv, §5). Das normalisierte Datum entsteht aus
> der ersten Capture-Group via der eingebauten Parser — so bleibt die Normalisierung an einer
> Stelle. Ungültige Regex sind bereits beim Config-Laden abgewiesen (Meilenstein 1).

- [ ] **Step 4: Tests laufen lassen (müssen bestehen)**

Run: `cd backend && uv run pytest tests/test_analyzer.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/src/zilpzalp/analyzer.py backend/tests/test_analyzer.py
git commit -m "feat(analyzer): apply optional config date_patterns additively"
```

---

## Task 10: Analyzer — strukturgestützte Label-Zuordnung

Jeder Datumskandidat ohne Label bekommt einen Kontext: Tabellenzelle (Nachbar-/Kopfzelle),
sonst Inline-Text davor, sonst zugehörige Überschrift. §4.3-Invariante bleibt gewahrt (Menge
unverändert, nur Labels werden angereichert).

**Files:**
- Modify: `backend/src/zilpzalp/analyzer.py`
- Test: `backend/tests/test_analyzer.py`

- [ ] **Step 1: Failing test schreiben (Mehrfach-Datumsdokument, §4.3 verbindlich)**

In `backend/tests/test_analyzer.py` ergänzen:

```python
def test_structural_labels_for_multiple_dates(tmp_path):
    doc = Document(blocks=[
        Block(kind="heading", text="Rechnung", page=1, bbox=(0, 800, 500, 820), level=1),
        Block(kind="paragraph", text="Rechnungsdatum: 15.01.2026", page=1, bbox=(0, 700, 500, 720)),
        Block(
            kind="table",
            text="Faellig am 01.02.2026",
            page=1,
            bbox=(0, 600, 500, 660),
            cells=[["Faellig am", "01.02.2026"]],
        ),
        Block(kind="paragraph", text="31.12.2025", page=1, bbox=(0, 500, 500, 520)),
    ])
    analysis = analyze(doc, _config(tmp_path))
    by_date = {c.normalized: c.label for c in analysis.date_candidates}

    assert by_date["2026-01-15"] == "Rechnungsdatum"      # Inline-Text vor dem Treffer
    assert by_date["2026-02-01"] == "Faellig am"          # Nachbarzelle in der Tabellenzeile
    assert by_date["2025-12-31"] == "Rechnung"            # Fallback: zugehoerige Ueberschrift
    # §4.3: nichts entfernt, alle drei Kandidaten vorhanden.
    assert len(analysis.date_candidates) == 3
```

- [ ] **Step 2: Test laufen lassen (muss fehlschlagen)**

Run: `cd backend && uv run pytest tests/test_analyzer.py::test_structural_labels_for_multiple_dates -v`
Expected: FAIL (Labels sind noch `None`).

- [ ] **Step 3: Label-Ableitung implementieren**

In `backend/src/zilpzalp/analyzer.py` Hilfsfunktionen ergänzen:

```python
_INLINE_LABEL = re.compile(r"([A-Za-zäöüÄÖÜ][A-Za-zäöüÄÖÜ ]{1,39}?)[:\s]*$")


def _inline_label(text_before: str) -> str | None:
    snippet = text_before.splitlines()[-1] if text_before.splitlines() else text_before
    m = _INLINE_LABEL.search(snippet.strip())
    return m.group(1).strip() if m else None


def _cell_label(cells: list[list[str]], hit: str) -> str | None:
    for r, row in enumerate(cells):
        for c, cell in enumerate(row):
            if hit in cell:
                if c > 0 and row[c - 1].strip():
                    return row[c - 1].strip()          # Nachbarzelle links
                if r > 0 and cells[0][c].strip():
                    return cells[0][c].strip()          # Kopfzelle der Spalte
    return None
```

`analyze` so erweitern, dass beim Bau jedes (nicht von `date_patterns` gelabelten) Kandidaten ein
Label gesetzt wird. Den eingebauten Zweig in der Block-Schleife ersetzen:

```python
    last_heading: str | None = None
    for block in document.blocks:
        if block.kind == "heading":
            last_heading = block.text.strip() or last_heading
        for start, _end, hit, d in _find_dates_in_text(block.text):
            if block.kind == "table" and block.cells:
                label = _cell_label(block.cells, hit)
            else:
                label = _inline_label(block.text[:start])
            label = label or last_heading
            candidates.append(
                DateCandidate(normalized=d.strftime(config.date_format), raw=hit, label=label)
            )
```

> `last_heading` muss **vor** der Block-Schleife initialisiert und `full_text`/`candidates` wie
> bisher behandelt werden. Der `date_patterns`-Zweig (Task 9) bleibt unverändert darunter — seine
> Labels stammen aus der Config und werden nicht überschrieben.

- [ ] **Step 4: Tests laufen lassen (müssen bestehen)**

Run: `cd backend && uv run pytest tests/test_analyzer.py -v`
Expected: PASS (alle Analyzer-Tests).

- [ ] **Step 5: Commit**

```bash
git add backend/src/zilpzalp/analyzer.py backend/tests/test_analyzer.py
git commit -m "feat(analyzer): derive structural labels for date candidates"
```

---

## Task 11: Analyzer — Absender- & Dokumenttyp-Heuristik

**Files:**
- Modify: `backend/src/zilpzalp/analyzer.py`
- Test: `backend/tests/test_analyzer.py`

- [ ] **Step 1: Failing test schreiben**

In `backend/tests/test_analyzer.py` ergänzen:

```python
def test_sender_and_doctype_heuristics(tmp_path):
    doc = Document(blocks=[
        Block(kind="paragraph", text="Stadtwerke Musterstadt GmbH", page=1, bbox=(0, 805, 500, 825)),
        Block(kind="heading", text="Rechnung", page=1, bbox=(0, 760, 200, 780), level=1),
        Block(kind="paragraph", text="Rechnungsdatum: 15.01.2026", page=1, bbox=(0, 700, 500, 720)),
    ])
    analysis = analyze(doc, _config(tmp_path))

    assert analysis.sender == "Stadtwerke Musterstadt GmbH"   # oberster Block auf Seite 1
    assert analysis.doctype == "Rechnung"                      # Heading aus Doctype-Vokabular
    assert "Rechnungsdatum: 15.01.2026" in analysis.full_text
```

- [ ] **Step 2: Test laufen lassen (muss fehlschlagen)**

Run: `cd backend && uv run pytest tests/test_analyzer.py::test_sender_and_doctype_heuristics -v`
Expected: FAIL (`sender`/`doctype` sind noch `None`).

- [ ] **Step 3: Heuristiken implementieren**

In `backend/src/zilpzalp/analyzer.py` ergänzen:

```python
_DOCTYPES = {
    "rechnung": "Rechnung", "mahnung": "Mahnung", "vertrag": "Vertrag",
    "bescheid": "Bescheid", "mitteilung": "Mitteilung", "kontoauszug": "Kontoauszug",
    "angebot": "Angebot", "kuendigung": "Kündigung", "kündigung": "Kündigung",
}


def _detect_sender(document: Document) -> str | None:
    page1 = [b for b in document.blocks if b.page == 1 and b.text.strip()]
    if not page1:
        return None
    top = max(page1, key=lambda b: b.bbox[3])     # groesster top-Wert = oberster Block
    return top.text.strip().splitlines()[0].strip()


def _detect_doctype(document: Document) -> str | None:
    headings = [b for b in document.blocks if b.kind == "heading"]
    for block in headings + document.blocks:       # zuerst Ueberschriften, dann beliebig
        low = block.text.lower()
        for key, canonical in _DOCTYPES.items():
            if key in low:
                return canonical
    return None
```

`analyze` am Ende anpassen:

```python
    return Analysis(
        date_candidates=candidates,
        sender=_detect_sender(document),
        doctype=_detect_doctype(document),
        full_text=full_text,
    )
```

- [ ] **Step 4: Tests laufen lassen (müssen bestehen)**

Run: `cd backend && uv run pytest tests/test_analyzer.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/src/zilpzalp/analyzer.py backend/tests/test_analyzer.py
git commit -m "feat(analyzer): heuristics for sender and doctype"
```

---

## Task 12: Suggestion — `Suggestion`-Typ + Pattern-Rendering (ohne Regeln)

**Files:**
- Create: `backend/src/zilpzalp/suggestion.py`
- Test: `backend/tests/test_suggestion.py`

- [ ] **Step 1: Failing test schreiben**

Create `backend/tests/test_suggestion.py`:

```python
from pathlib import Path

from zilpzalp.analyzer import Analysis, DateCandidate
from zilpzalp.config import load_config
from zilpzalp.suggestion import suggest


def _config(tmp_path: Path, extra: str = "") -> "object":
    (tmp_path / "inbox").mkdir(exist_ok=True)
    (tmp_path / "error").mkdir(exist_ok=True)
    cfg = tmp_path / "config.yaml"
    cfg.write_text(
        f"""
paths:
  watchfolder: {tmp_path / "inbox"}
  error_folder: {tmp_path / "error"}
original_handling: keep
summary_mode: never
default_pattern: "{{date}}__{{sender}}_{{doctype}}_{{description}}"
date_format: "%Y-%m-%d"
{extra}
""",
        encoding="utf-8",
    )
    return load_config(cfg)


def test_renders_default_pattern_and_preselects_first_date(tmp_path):
    analysis = Analysis(
        date_candidates=[
            DateCandidate(normalized="2026-01-15", raw="15.01.2026", label="Rechnungsdatum"),
            DateCandidate(normalized="2026-02-01", raw="01.02.2026", label="Faellig am"),
        ],
        sender="Stadtwerke",
        doctype="Rechnung",
        full_text="...",
    )
    s = suggest(analysis, _config(tmp_path))

    assert s.preselected_date_index == 0
    assert s.filename == "2026-01-15__Stadtwerke_Rechnung_.pdf"
    assert len(s.date_candidates) == 2          # §4.3: alle Kandidaten durchgereicht
```

- [ ] **Step 2: Test laufen lassen (muss fehlschlagen)**

Run: `cd backend && uv run pytest tests/test_suggestion.py -v`
Expected: FAIL mit `ModuleNotFoundError: No module named 'zilpzalp.suggestion'`

- [ ] **Step 3: Suggestion-Grundgerüst implementieren**

Create `backend/src/zilpzalp/suggestion.py`:

```python
from __future__ import annotations

from dataclasses import dataclass, field
from pathlib import Path

from zilpzalp.analyzer import Analysis, DateCandidate
from zilpzalp.config import Config


@dataclass(frozen=True)
class Suggestion:
    filename: str
    date_candidates: list[DateCandidate] = field(default_factory=list)
    preselected_date_index: int | None = None
    sender: str = ""
    doctype: str = ""
    description: str = ""
    pattern_name: str | None = None
    target_paths: list[Path] = field(default_factory=list)


def _resolve_pattern(config: Config, pattern_name: str | None) -> str:
    if pattern_name:
        for pattern in config.patterns:
            if pattern.name == pattern_name:
                return pattern.template
    return config.default_pattern


def _preselect(candidates: list[DateCandidate], preferred_label: str | None) -> int | None:
    if not candidates:
        return None
    if preferred_label:
        wanted = preferred_label.lower()
        for i, c in enumerate(candidates):
            if c.label and wanted in c.label.lower():
                return i
    return 0


def suggest(analysis: Analysis, config: Config) -> Suggestion:
    sender = analysis.sender or ""
    doctype = analysis.doctype or ""
    description = ""
    pattern_name: str | None = None

    idx = _preselect(analysis.date_candidates, None)
    date_str = analysis.date_candidates[idx].normalized if idx is not None else ""
    template = _resolve_pattern(config, pattern_name)
    filename = (
        template.format(date=date_str, sender=sender, doctype=doctype, description=description)
        + ".pdf"
    )
    return Suggestion(
        filename=filename,
        date_candidates=list(analysis.date_candidates),
        preselected_date_index=idx,
        sender=sender,
        doctype=doctype,
        description=description,
        pattern_name=pattern_name,
        target_paths=[],
    )
```

- [ ] **Step 4: Test laufen lassen (muss bestehen)**

Run: `cd backend && uv run pytest tests/test_suggestion.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/src/zilpzalp/suggestion.py backend/tests/test_suggestion.py
git commit -m "feat(suggestion): Suggestion type with default pattern rendering"
```

---

## Task 13: Suggestion — Regel-Matching, `apply`-Overrides, `preferred_date`

**Files:**
- Modify: `backend/src/zilpzalp/suggestion.py`
- Test: `backend/tests/test_suggestion.py`

- [ ] **Step 1: Failing test schreiben**

In `backend/tests/test_suggestion.py` ergänzen:

```python
RULES = """
patterns:
  - name: standard
    template: "{date}__{sender}_{doctype}_{description}"
rules:
  - name: Stromrechnung Stadtwerke
    match:
      sender_contains: "Stadtwerke"
      keywords_any: ["Stromabschlag", "Abschlag"]
    apply:
      sender: "Stadtwerke"
      doctype: "Rechnung"
      description: "Stromabschlag"
      pattern: standard
      preferred_date: faellig
"""


def test_matching_rule_applies_overrides_and_preferred_date(tmp_path):
    analysis = Analysis(
        date_candidates=[
            DateCandidate(normalized="2026-01-15", raw="15.01.2026", label="Rechnungsdatum"),
            DateCandidate(normalized="2026-02-01", raw="01.02.2026", label="Faellig am"),
        ],
        sender="Stadtwerke Musterstadt GmbH",
        doctype=None,
        full_text="... Stromabschlag Januar ...",
    )
    s = suggest(analysis, _config(tmp_path, RULES))

    assert s.description == "Stromabschlag"               # apply-Override
    assert s.preselected_date_index == 1                  # preferred_date "faellig" -> "Faellig am"
    assert s.filename == "2026-02-01__Stadtwerke_Rechnung_Stromabschlag.pdf"


def test_first_matching_rule_wins(tmp_path):
    two_rules = """
rules:
  - name: generisch
    match:
      sender_contains: "Stadtwerke"
    apply:
      doctype: "Allgemein"
  - name: spezifisch
    match:
      sender_contains: "Stadtwerke"
    apply:
      doctype: "Rechnung"
"""
    analysis = Analysis(
        date_candidates=[DateCandidate(normalized="2026-01-15", raw="15.01.2026")],
        sender="Stadtwerke",
        full_text="x",
    )
    s = suggest(analysis, _config(tmp_path, two_rules))
    assert s.doctype == "Allgemein"                       # erste Uebereinstimmung gewinnt
```

- [ ] **Step 2: Tests laufen lassen (müssen fehlschlagen)**

Run: `cd backend && uv run pytest tests/test_suggestion.py -k rule -v`
Expected: FAIL (Regeln werden noch nicht ausgewertet).

- [ ] **Step 3: Regel-Matching implementieren**

In `backend/src/zilpzalp/suggestion.py` ergänzen:

```python
def _rule_matches(rule: dict, analysis: Analysis) -> bool:
    match = rule.get("match", {}) or {}
    sender_contains = match.get("sender_contains")
    if sender_contains and sender_contains.lower() not in (analysis.sender or "").lower():
        return False
    keywords_any = match.get("keywords_any")
    if keywords_any and not any(
        k.lower() in analysis.full_text.lower() for k in keywords_any
    ):
        return False
    return True


def _first_matching_rule(analysis: Analysis, config: Config) -> dict | None:
    for rule in config.rules:
        if _rule_matches(rule, analysis):
            return rule
    return None
```

`suggest` so anpassen, dass eine greifende Regel die Felder überschreibt und `preferred_date`
die Vorauswahl steuert:

```python
def suggest(analysis: Analysis, config: Config) -> Suggestion:
    sender = analysis.sender or ""
    doctype = analysis.doctype or ""
    description = ""
    pattern_name: str | None = None
    preferred_label: str | None = None

    rule = _first_matching_rule(analysis, config)
    if rule:
        apply = rule.get("apply", {}) or {}
        sender = apply.get("sender", sender)
        doctype = apply.get("doctype", doctype)
        description = apply.get("description", description)
        pattern_name = apply.get("pattern", pattern_name)
        preferred_label = apply.get("preferred_date")

    idx = _preselect(analysis.date_candidates, preferred_label)
    date_str = analysis.date_candidates[idx].normalized if idx is not None else ""
    template = _resolve_pattern(config, pattern_name)
    filename = (
        template.format(date=date_str, sender=sender, doctype=doctype, description=description)
        + ".pdf"
    )
    return Suggestion(
        filename=filename,
        date_candidates=list(analysis.date_candidates),
        preselected_date_index=idx,
        sender=sender,
        doctype=doctype,
        description=description,
        pattern_name=pattern_name,
        target_paths=[],
    )
```

- [ ] **Step 4: Tests laufen lassen (müssen bestehen)**

Run: `cd backend && uv run pytest tests/test_suggestion.py -v`
Expected: PASS (alle Suggestion-Tests).

- [ ] **Step 5: Commit**

```bash
git add backend/src/zilpzalp/suggestion.py backend/tests/test_suggestion.py
git commit -m "feat(suggestion): rule matching, apply overrides, preferred_date"
```

---

## Task 14: Suggestion — Zielordner-Auflösung (Regel-Targets + Standard-Targets)

**Files:**
- Modify: `backend/src/zilpzalp/suggestion.py`
- Test: `backend/tests/test_suggestion.py`

- [ ] **Step 1: Failing test schreiben**

In `backend/tests/test_suggestion.py` ergänzen:

```python
TARGETS = """
targets:
  - name: Finanzen
    path: /targets/finanzen
    default: false
  - name: Allgemein
    path: /targets/allgemein
    default: true
rules:
  - name: zu Finanzen
    match:
      sender_contains: "Stadtwerke"
    apply:
      targets: ["Finanzen"]
"""


def test_rule_targets_resolved_to_paths(tmp_path):
    analysis = Analysis(
        date_candidates=[DateCandidate(normalized="2026-01-15", raw="15.01.2026")],
        sender="Stadtwerke",
        full_text="x",
    )
    s = suggest(analysis, _config(tmp_path, TARGETS))
    assert [str(p) for p in s.target_paths] == ["/targets/finanzen"]


def test_default_targets_when_no_rule_targets(tmp_path):
    analysis = Analysis(
        date_candidates=[DateCandidate(normalized="2026-01-15", raw="15.01.2026")],
        sender="Unbekannt",
        full_text="x",
    )
    s = suggest(analysis, _config(tmp_path, TARGETS))
    assert [str(p) for p in s.target_paths] == ["/targets/allgemein"]   # default: true
```

- [ ] **Step 2: Tests laufen lassen (müssen fehlschlagen)**

Run: `cd backend && uv run pytest tests/test_suggestion.py -k target -v`
Expected: FAIL (`target_paths` ist noch immer leer).

- [ ] **Step 3: Zielordner-Auflösung implementieren**

In `backend/src/zilpzalp/suggestion.py` ergänzen:

```python
def _resolve_targets(config: Config, names: list[str]) -> list[Path]:
    if names:
        wanted = set(names)
        return [t.path for t in config.targets if t.name in wanted]
    return [t.path for t in config.targets if t.default]
```

In `suggest`: `target_names` aus der Regel ziehen und auflösen. Den `rule`-Block und den
`Suggestion`-Aufbau anpassen:

```python
    target_names: list[str] = []
    if rule:
        apply = rule.get("apply", {}) or {}
        # ... bestehende Overrides ...
        target_names = apply.get("targets", []) or []
```

```python
        target_paths=_resolve_targets(config, target_names),
```

- [ ] **Step 4: Tests laufen lassen (müssen bestehen)**

Run: `cd backend && uv run pytest tests/test_suggestion.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add backend/src/zilpzalp/suggestion.py backend/tests/test_suggestion.py
git commit -m "feat(suggestion): resolve rule and default target folders"
```

---

## Task 15: Integrationstest — Document → analyze → suggest (Mehrfach-Datum)

Verifiziert den Modul-Zusammenschluss an einem realistischen Mehrfach-Datumsdokument (Risiko
17.1) — ohne JVM, mit Hand-`Document`.

**Files:**
- Create: `backend/tests/test_analysis_pipeline.py`

- [ ] **Step 1: Failing test schreiben**

Create `backend/tests/test_analysis_pipeline.py`:

```python
from pathlib import Path

from zilpzalp.analyzer import analyze
from zilpzalp.config import load_config
from zilpzalp.document import Block, Document
from zilpzalp.suggestion import suggest


def _config(tmp_path: Path) -> "object":
    (tmp_path / "inbox").mkdir()
    (tmp_path / "error").mkdir()
    cfg = tmp_path / "config.yaml"
    cfg.write_text(
        f"""
paths:
  watchfolder: {tmp_path / "inbox"}
  error_folder: {tmp_path / "error"}
original_handling: keep
summary_mode: never
default_pattern: "{{date}}__{{sender}}_{{doctype}}_{{description}}"
date_format: "%Y-%m-%d"
rules:
  - name: Stadtwerke
    match:
      sender_contains: "Stadtwerke"
      keywords_any: ["Stromabschlag"]
    apply:
      sender: "Stadtwerke"
      doctype: "Rechnung"
      description: "Stromabschlag"
      preferred_date: rechnungsdatum
""",
        encoding="utf-8",
    )
    return load_config(cfg)


def test_full_analysis_pipeline_invoice(tmp_path):
    doc = Document(blocks=[
        Block(kind="paragraph", text="Stadtwerke Musterstadt GmbH", page=1, bbox=(0, 810, 500, 830)),
        Block(kind="heading", text="Rechnung", page=1, bbox=(0, 770, 200, 790), level=1),
        Block(kind="paragraph", text="Rechnungsdatum: 15.01.2026", page=1, bbox=(0, 720, 500, 740)),
        Block(kind="paragraph", text="Faellig am: 01.02.2026", page=1, bbox=(0, 690, 500, 710)),
        Block(kind="paragraph", text="Stromabschlag Januar", page=1, bbox=(0, 660, 500, 680)),
    ])
    analysis = analyze(doc, _config(tmp_path))
    suggestion = suggest(analysis, _config(tmp_path))

    # §4.3: beide Datumsangaben sichtbar, korrekt gelabelt.
    labels = {c.label: c.normalized for c in analysis.date_candidates}
    assert labels["Rechnungsdatum"] == "2026-01-15"
    assert labels["Faellig am"] == "2026-02-01"
    assert len(analysis.date_candidates) == 2

    # Regel greift -> Vorauswahl Rechnungsdatum, fertiger Name.
    assert suggestion.preselected_date_index == 0
    assert suggestion.filename == "2026-01-15__Stadtwerke_Rechnung_Stromabschlag.pdf"
```

- [ ] **Step 2: Test laufen lassen**

Run: `cd backend && uv run pytest tests/test_analysis_pipeline.py -v`
Expected: PASS (alle Bausteine existieren bereits). Bei FAIL den jeweils zuständigen Baustein
(analyzer/suggestion) korrigieren.

- [ ] **Step 3: Gesamte Suite + Linting**

Run: `cd backend && uv run pytest -v && uv run ruff check src tests`
Expected: Alle Tests PASS (inkl. `integration`), ruff ohne Befund.

- [ ] **Step 4: Commit**

```bash
git add backend/tests/test_analysis_pipeline.py
git commit -m "test(analysis): end-to-end Document->analyze->suggest for multi-date invoice"
```

---

## Self-Review (vom Plan-Autor durchgeführt)

**Spec-Abdeckung:**
- §3 Komponenten (`extractor`/`analyzer`/`suggestion`, Kern-Designregel reine Funktionen) → Tasks 4–14. ✅
- §3.1 Dokumentmodell (`Document`/`Block`) → Task 3. ✅
- §4.1 Datenfluss (ODL → Temp-Cleanup → kein Text → Fehler) → Task 6. ✅
- §4.3 **alle** Datumskandidaten, Label, Vorauswahl ändert nie die Menge → Tasks 7–10, 12, 13, 15. ✅
- §5 Config (`date_patterns` additiv, Regelpriorität erste Übereinstimmung, `preferred_date`,
  Pattern, Targets) → Tasks 9, 13, 14. ✅
- §7 Teststrategie (analyzer mit Hand-Fixtures, extractor gegen JVM, Datensparsamkeit/Temp) →
  Tasks 6–15. ✅
- §6 Fehler „kein Text" → Task 6 (`ExtractionError`). ✅

**Bewusst NICHT in diesem Meilenstein** (gehören zu späteren laut Roadmap): Verschieben nach
`error/` (Task `processor`/`watcher`, M3/M4), Queue-Eintrag-Status (M4), Web-UI-Datumsliste (M5).
`extract()` signalisiert den Fehler nur via `ExtractionError`; das Einsortieren nach `error/`
macht der spätere Aufrufer.

**Platzhalter-Scan:** Keine TBD/TODO. Die einzigen „anpassen falls"-Hinweise betreffen reale,
extern bestimmte ODL-Feldnamen (Task 2 hält sie als Capture fest, Tests laufen gegen das echte
JSON) — das ist bewusste Verifikation, kein Platzhalter.

**Typ-Konsistenz:** `Block`/`Document` (Task 3) ↔ `document_from_odl`/`extract` (Tasks 4–6);
`DateCandidate`/`Analysis` (Task 7) ↔ `suggest` (Tasks 12–14); `Suggestion.preselected_date_index`,
`target_paths`, `filename` konsistent über Tasks 12–15. ✅

---

## Execution Handoff

**Plan complete and saved to `docs/superpowers/plans/2026-06-13-1642-analyse-kern.md`. Zwei Ausführungsoptionen:**

**1. Subagent-Driven (empfohlen)** — frischer Subagent pro Task, Review zwischen den Tasks, schnelle Iteration.

**2. Inline-Ausführung** — Tasks in dieser Session per `executing-plans`, Batch mit Checkpoints.

**Laut Roadmap-Arbeitsweise wird die Umsetzung in einer frischen Session gestartet. Welche Vorgehensweise?**
