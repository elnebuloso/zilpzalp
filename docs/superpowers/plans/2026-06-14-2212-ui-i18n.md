# UI-Mehrsprachigkeit (DE/EN) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Alle UI-Texte aus Templates und Python-Code in JSON-Kataloge (DE/EN) auslagern, mit einem `t()`-Helfer, einem Sprachumschalter (Cookie, Browser-Default) und einem korrigierten, sprach-unabhängigen Datumshandling.

**Architecture:** Neues Modul `web/i18n.py` lädt JSON-Kataloge aus `web/locales/` und bietet `translate(key, lang, **vars)` + `resolve_language(request)`. In `routes.py` löst `_base_context()` die Sprache pro Request auf und legt ein gebundenes `t` sowie `lang` in den Template-Context. Templates rufen `{{ t("key") }}`; Status-/Label-Konstanten und Flash-Texte wandern in die Kataloge.

**Tech Stack:** Python 3.12, FastAPI, Jinja2, htmx, pytest.

Spec: [docs/superpowers/specs/2026-06-14-2212-ui-i18n-design.md](../specs/2026-06-14-2212-ui-i18n-design.md)

---

## File Structure

- `backend/src/zilpzalp/web/i18n.py` (Create) — Katalog-Laden, `translate`, `resolve_language`, `SUPPORTED`, `DEFAULT`.
- `backend/src/zilpzalp/web/locales/de.json` (Create) — deutscher Katalog (Quellsprache).
- `backend/src/zilpzalp/web/locales/en.json` (Create) — englischer Katalog.
- `backend/src/zilpzalp/web/routes.py` (Modify) — `t`/`lang` im Context, `/lang/{code}`-Route, Status/Label-Konstanten externalisieren, Flash-Texte, `_normalize_date`-Fix, `german_date` entfernen.
- `backend/src/zilpzalp/web/templates/*.html` (Modify) — alle Strings auf `{{ t(...) }}`, `<html lang>`, Sprachumschalter.
- `backend/src/zilpzalp/web/static/app.js` (Modify) — Hint-Strings aus `data-*`-Attributen lesen.
- `backend/src/zilpzalp/web/static/styles.css` (Modify) — Stil für den Sprachumschalter.
- `backend/tests/test_i18n.py` (Create) — Unit-Tests für `translate`/`resolve_language`/Katalog-Vollständigkeit.
- `backend/tests/test_routes.py` (Modify) — Sprachwechsel-Tests + Datumsformat-Test.

**Working directory for all commands:** `backend/`

---

## Task 1: i18n-Modul + Kataloge

**Files:**
- Create: `backend/src/zilpzalp/web/i18n.py`
- Create: `backend/src/zilpzalp/web/locales/de.json`
- Create: `backend/src/zilpzalp/web/locales/en.json`
- Test: `backend/tests/test_i18n.py`

- [ ] **Step 1: Write the failing test**

Create `backend/tests/test_i18n.py`:

```python
import json
from pathlib import Path

import pytest

from zilpzalp.web import i18n

LOCALES = Path(i18n.__file__).parent / "locales"


def test_translate_returns_language_string():
    assert i18n.translate("nav.overview", "de") == "Übersicht"
    assert i18n.translate("nav.overview", "en") == "Overview"


def test_translate_falls_back_to_default_language():
    # An unknown language resolves nothing; default (de) is used.
    assert i18n.translate("nav.overview", "fr") == "Übersicht"


def test_translate_unknown_key_returns_key():
    assert i18n.translate("does.not.exist", "de") == "does.not.exist"


def test_translate_interpolates_vars():
    out = i18n.translate("toast.filed", "de", filename="x.pdf")
    assert "x.pdf" in out


def test_resolve_language_prefers_cookie():
    req = _fake_request(cookies={"lang": "en"}, accept="de")
    assert i18n.resolve_language(req) == "en"


def test_resolve_language_uses_accept_language_when_no_cookie():
    req = _fake_request(cookies={}, accept="en-US,en;q=0.9,de;q=0.8")
    assert i18n.resolve_language(req) == "en"


def test_resolve_language_falls_back_to_default():
    req = _fake_request(cookies={}, accept="fr-FR,fr;q=0.9")
    assert i18n.resolve_language(req) == "de"


def test_resolve_language_ignores_unsupported_cookie():
    req = _fake_request(cookies={"lang": "fr"}, accept="en")
    assert i18n.resolve_language(req) == "en"


def test_catalogs_have_identical_keys():
    de = json.loads((LOCALES / "de.json").read_text(encoding="utf-8"))
    en = json.loads((LOCALES / "en.json").read_text(encoding="utf-8"))
    assert set(de) == set(en), (
        f"only de: {set(de) - set(en)} | only en: {set(en) - set(de)}"
    )


class _FakeURL:
    def __init__(self, path="/"):
        self.path = path


class _FakeRequest:
    def __init__(self, cookies, headers):
        self.cookies = cookies
        self.headers = headers
        self.url = _FakeURL()


def _fake_request(cookies, accept):
    return _FakeRequest(cookies, {"accept-language": accept})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_i18n.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'zilpzalp.web.i18n'`.

- [ ] **Step 3: Create the German catalog**

Create `backend/src/zilpzalp/web/locales/de.json`:

```json
{
  "nav.overview": "Übersicht",
  "nav.queue": "Warteschlange",
  "nav.config": "Konfiguration",
  "aria.theme_toggle": "Darstellung umschalten",
  "aria.language": "Sprache wählen",
  "aria.close": "schließen",

  "status.pending": "wartet",
  "status.analyzing": "Analyse",
  "status.ready": "bereit",
  "status.error": "Fehler",

  "original.move": "verschieben",
  "original.delete": "löschen",
  "original.keep": "behalten",

  "summary_mode.always": "immer",
  "summary_mode.on_conflict": "bei Konflikt",
  "summary_mode.never": "nie",

  "toast.filed": "„{filename}“ wurde abgelegt.",
  "toast.file_error": "Fehler bei der Ablage: {error}",

  "overview.title": "Übersicht",
  "overview.operations": "Betriebsangaben",
  "overview.watchfolder": "Überwachter Ordner",
  "overview.config_file": "Konfigurationsdatei",
  "overview.original_handling": "Umgang mit Original",
  "overview.summary": "Zusammenfassung",
  "overview.targets": "Zielordner",
  "overview.patterns": "Namensmuster",
  "overview.rules": "Regeln",
  "overview.recent": "Jüngste Dokumente",
  "overview.show_all": "Alle anzeigen",
  "overview.empty_title": "Die Warteschlange ist leer",
  "overview.empty_sub": "Neue Dokumente erscheinen hier, sobald sie im überwachten Ordner liegen.",

  "queue.title": "Warteschlange",
  "queue.subtitle": "Dokumente im überwachten Ordner, die noch nicht bestätigt verarbeitet wurden.",
  "queue.empty_title": "Keine Dokumente in der Warteschlange",
  "queue.empty_sub": "Sobald eine Datei im überwachten Ordner liegt, erscheint sie hier von allein.",
  "queue.analyzing": "wird ausgewertet …",
  "queue.waiting": "wartet auf Analyse",

  "action.review": "Prüfen",
  "action.cancel": "Abbrechen",
  "action.confirm": "Bestätigen",
  "action.execute": "Ausführen",

  "review.back": "Zurück zur Warteschlange",
  "review.title": "Dokument prüfen",
  "review.choose_date": "Datum wählen",
  "review.choose_date_hint": "Alle gefundenen Datumsangaben. Eine ist vorgewählt — bei Bedarf wechseln.",
  "review.manual_date": "Datum manuell eingeben",
  "review.fields": "Felder",
  "review.sender": "Absender",
  "review.doctype": "Dokumenttyp",
  "review.pattern": "Namensmuster",
  "review.description": "Beschreibung",
  "review.targets": "Zielordner",
  "review.targets_hint": "· mindestens einer · empfohlen vorgewählt",
  "review.final_name": "Endgültiger Dateiname",
  "review.meta_pattern": "Muster",
  "review.meta_original": "Original",
  "review.msg_date_required": "Bitte ein Datum wählen.",
  "review.msg_target_required": "Mindestens einen Zielordner wählen.",

  "config.title": "Konfiguration",
  "config.subtitle": "Konfiguration als YAML. Wird beim Speichern geprüft.",
  "config.errors_title": "Konfiguration nicht übernommen — die bisherige bleibt in Kraft",
  "config.saved": "Gespeichert",
  "config.save": "Speichern & prüfen",
  "config.footnote": "Gilt für neu hinzukommende Dokumente; bereits ausgewertete behalten ihren Vorschlag. Für eine Neubewertung erneut in den überwachten Ordner legen.",

  "summary.title": "Zusammenfassung",
  "summary.subtitle": "Prüfen Sie die Ablage. Keine Datei wird ohne diese Bestätigung verändert.",
  "summary.conflict_strong": "Namenskonflikt.",
  "summary.conflict_rest": "Es wird kein Zusatz automatisch ergänzt — bitte den Namen in der Prüfung ändern. Die Ausführung ist gesperrt.",
  "summary.source": "Quelldatei",
  "summary.final_name": "Endgültiger Name",
  "summary.destinations": "Ablageorte",
  "summary.exists": "· existiert bereits",
  "summary.original": "Original"
}
```

- [ ] **Step 4: Create the English catalog**

Create `backend/src/zilpzalp/web/locales/en.json`:

```json
{
  "nav.overview": "Overview",
  "nav.queue": "Queue",
  "nav.config": "Configuration",
  "aria.theme_toggle": "Toggle appearance",
  "aria.language": "Choose language",
  "aria.close": "close",

  "status.pending": "waiting",
  "status.analyzing": "Analyzing",
  "status.ready": "ready",
  "status.error": "Error",

  "original.move": "move",
  "original.delete": "delete",
  "original.keep": "keep",

  "summary_mode.always": "always",
  "summary_mode.on_conflict": "on conflict",
  "summary_mode.never": "never",

  "toast.filed": "“{filename}” has been filed.",
  "toast.file_error": "Error while filing: {error}",

  "overview.title": "Overview",
  "overview.operations": "Operational details",
  "overview.watchfolder": "Watched folder",
  "overview.config_file": "Configuration file",
  "overview.original_handling": "Original handling",
  "overview.summary": "Summary",
  "overview.targets": "Target folders",
  "overview.patterns": "Naming patterns",
  "overview.rules": "Rules",
  "overview.recent": "Recent documents",
  "overview.show_all": "Show all",
  "overview.empty_title": "The queue is empty",
  "overview.empty_sub": "New documents appear here as soon as they are placed in the watched folder.",

  "queue.title": "Queue",
  "queue.subtitle": "Documents in the watched folder that have not yet been confirmed and processed.",
  "queue.empty_title": "No documents in the queue",
  "queue.empty_sub": "As soon as a file is placed in the watched folder, it appears here automatically.",
  "queue.analyzing": "analyzing …",
  "queue.waiting": "awaiting analysis",

  "action.review": "Review",
  "action.cancel": "Cancel",
  "action.confirm": "Confirm",
  "action.execute": "Execute",

  "review.back": "Back to the queue",
  "review.title": "Review document",
  "review.choose_date": "Choose date",
  "review.choose_date_hint": "All detected dates. One is preselected — switch if needed.",
  "review.manual_date": "Enter date manually",
  "review.fields": "Fields",
  "review.sender": "Sender",
  "review.doctype": "Document type",
  "review.pattern": "Naming pattern",
  "review.description": "Description",
  "review.targets": "Target folders",
  "review.targets_hint": "· at least one · recommended preselected",
  "review.final_name": "Final file name",
  "review.meta_pattern": "Pattern",
  "review.meta_original": "Original",
  "review.msg_date_required": "Please choose a date.",
  "review.msg_target_required": "Choose at least one target folder.",

  "config.title": "Configuration",
  "config.subtitle": "Configuration as YAML. Validated on save.",
  "config.errors_title": "Configuration not applied — the previous one remains in effect",
  "config.saved": "Saved",
  "config.save": "Save & validate",
  "config.footnote": "Applies to newly arriving documents; already analyzed ones keep their suggestion. To re-evaluate, place the file in the watched folder again.",

  "summary.title": "Summary",
  "summary.subtitle": "Review the filing. No file is changed without this confirmation.",
  "summary.conflict_strong": "Naming conflict.",
  "summary.conflict_rest": "No suffix is added automatically — please change the name in the review. Execution is locked.",
  "summary.source": "Source file",
  "summary.final_name": "Final name",
  "summary.destinations": "Destinations",
  "summary.exists": "· already exists",
  "summary.original": "Original"
}
```

- [ ] **Step 5: Implement the i18n module**

Create `backend/src/zilpzalp/web/i18n.py`:

```python
from __future__ import annotations

import json
from pathlib import Path

SUPPORTED: tuple[str, ...] = ("de", "en")
DEFAULT = "de"

_LOCALES_DIR = Path(__file__).parent / "locales"
_CATALOGS: dict[str, dict[str, str]] = {
    lang: json.loads((_LOCALES_DIR / f"{lang}.json").read_text(encoding="utf-8"))
    for lang in SUPPORTED
}


def translate(key: str, lang: str, **vars: object) -> str:
    """Look up ``key`` in ``lang``; fall back to DEFAULT, then to the key itself."""
    catalog = _CATALOGS.get(lang) or _CATALOGS[DEFAULT]
    text = catalog.get(key)
    if text is None:
        text = _CATALOGS[DEFAULT].get(key, key)
    if vars:
        return text.format(**vars)
    return text


def resolve_language(request) -> str:
    """Pick the UI language: cookie ``lang`` wins, else Accept-Language, else DEFAULT."""
    cookie = request.cookies.get("lang")
    if cookie in SUPPORTED:
        return cookie
    header = request.headers.get("accept-language", "")
    for part in header.split(","):
        code = part.split(";")[0].strip().lower()[:2]
        if code in SUPPORTED:
            return code
    return DEFAULT
```

- [ ] **Step 6: Run tests to verify they pass**

Run: `python -m pytest tests/test_i18n.py -v`
Expected: PASS (all 9 tests).

- [ ] **Step 7: Commit**

```bash
git add src/zilpzalp/web/i18n.py src/zilpzalp/web/locales/ tests/test_i18n.py
git commit -m "feat(web): add i18n catalogs and translate/resolve_language helpers"
```

---

## Task 2: Sprache in den Context + Umschalter + `/lang`-Route

**Files:**
- Modify: `backend/src/zilpzalp/web/routes.py`
- Modify: `backend/src/zilpzalp/web/templates/base.html`
- Modify: `backend/src/zilpzalp/web/static/styles.css`
- Test: `backend/tests/test_routes.py`

- [ ] **Step 1: Write the failing tests**

Append to `backend/tests/test_routes.py`:

```python
def test_language_cookie_switches_nav_to_english(client):
    response = client.get("/", cookies={"lang": "en"})
    assert response.status_code == 200
    body = response.text
    assert "Overview" in body
    assert "Operational details" in body
    assert 'lang="en"' in body


def test_default_language_is_german(client):
    response = client.get("/")
    assert "Übersicht" in response.text
    assert 'lang="de"' in response.text


def test_set_language_sets_cookie_and_redirects(client):
    response = client.get("/lang/en?next=/queue", follow_redirects=False)
    assert response.status_code == 303
    assert response.headers["location"] == "/queue"
    assert "lang=en" in response.headers.get("set-cookie", "")


def test_set_language_rejects_unknown_code(client):
    response = client.get("/lang/fr", follow_redirects=False)
    assert response.status_code == 303
    assert "lang=" not in response.headers.get("set-cookie", "")


def test_set_language_ignores_external_next(client):
    response = client.get("/lang/en?next=https://evil.test", follow_redirects=False)
    assert response.headers["location"] == "/"
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `python -m pytest tests/test_routes.py -k "language" -v`
Expected: FAIL — English strings missing / `/lang` route returns 404.

- [ ] **Step 3: Wire language into routes**

In `backend/src/zilpzalp/web/routes.py`, add to the imports near the top (after the existing `from zilpzalp.web.naming import render_filename` line):

```python
from zilpzalp.web.i18n import SUPPORTED, resolve_language, translate
```

Replace the `_base_context` function with:

```python
def _base_context(request: Request, active: str) -> dict:
    queue: Queue = request.app.state.queue
    counts = _counts(queue)
    lang = resolve_language(request)
    return {
        "active": active,
        "open_count": counts["ready"],
        "counts": counts,
        "flash": request.query_params.get("flash"),
        "flash_kind": request.query_params.get("kind", "ok"),
        "lang": lang,
        "t": lambda key, **kw: translate(key, lang, **kw),
    }
```

Add the language route immediately after the `router = APIRouter()` / `templates = ...` block (anywhere among the route definitions is fine; place it right before `@router.get("/")`):

```python
@router.get("/lang/{code}")
def set_language(code: str, next: str = "/"):
    target = next if next.startswith("/") else "/"
    response = RedirectResponse(target, status_code=303)
    if code in SUPPORTED:
        response.set_cookie("lang", code, max_age=31_536_000, samesite="lax")
    return response
```

- [ ] **Step 4: Add the switcher and dynamic lang attribute to base.html**

In `backend/src/zilpzalp/web/templates/base.html`:

Change line 2 from `<html lang="de">` to:

```html
<html lang="{{ lang }}">
```

Replace the navigation labels and add the switcher. Replace the `<nav class="nav">…</nav>` block with:

```html
        <nav class="nav">
          <a class="nav-item {% if active == 'overview' %}active{% endif %}" href="/">
            {% if active == 'overview' %}<span class="nav-dot"></span>{% endif %}{{ t('nav.overview') }}
          </a>
          <a class="nav-item {% if active == 'queue' %}active{% endif %}" href="/queue">
            {% if active == 'queue' %}<span class="nav-dot"></span>{% endif %}{{ t('nav.queue') }}
            {% if open_count and open_count > 0 %}<span class="nav-count">{{ open_count }}</span>{% endif %}
          </a>
          <a class="nav-item {% if active == 'config' %}active{% endif %}" href="/config">
            {% if active == 'config' %}<span class="nav-dot"></span>{% endif %}{{ t('nav.config') }}
          </a>
        </nav>
```

Replace the `<div class="header-spacer"></div>` line and the theme-toggle button with the switcher followed by the (now translated) toggle:

```html
        <div class="header-spacer"></div>
        <div class="lang-switch" role="group" aria-label="{{ t('aria.language') }}">
          {% for code in ['de', 'en'] %}
          <a class="lang-opt {% if lang == code %}active{% endif %}" href="/lang/{{ code }}?next={{ request.url.path | urlencode }}">{{ code | upper }}</a>
          {% endfor %}
        </div>
        <button class="theme-toggle" data-theme-toggle aria-label="{{ t('aria.theme_toggle') }}">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" style="width:18px;height:18px"><circle cx="12" cy="12" r="4.2"/><path d="M12 2.5v2M12 19.5v2M2.5 12h2M19.5 12h2M5 5l1.4 1.4M17.6 17.6L19 19M19 5l-1.4 1.4M6.4 17.6L5 19"/></svg>
        </button>
```

- [ ] **Step 5: Add switcher styles**

Append to `backend/src/zilpzalp/web/static/styles.css`:

```css
/* Language switcher */
.lang-switch { display: flex; gap: 2px; margin-right: 8px; }
.lang-opt {
  font-size: 12px; font-weight: 600; letter-spacing: 0.03em;
  padding: 4px 8px; border-radius: 6px;
  color: var(--text-3); text-decoration: none;
}
.lang-opt:hover { color: var(--text); }
.lang-opt.active { color: var(--accent); background: var(--accent-bg); }
```

- [ ] **Step 6: Run tests to verify they pass**

Run: `python -m pytest tests/test_routes.py -k "language" -v`
Expected: PASS (5 tests).

- [ ] **Step 7: Commit**

```bash
git add src/zilpzalp/web/routes.py src/zilpzalp/web/templates/base.html src/zilpzalp/web/static/styles.css tests/test_routes.py
git commit -m "feat(web): add language switcher, /lang route and per-request language"
```

---

## Task 3: Status- und Label-Konstanten externalisieren

**Files:**
- Modify: `backend/src/zilpzalp/web/routes.py`
- Modify: `backend/src/zilpzalp/web/templates/_overview.html`
- Modify: `backend/src/zilpzalp/web/templates/_queue_list.html`
- Modify: `backend/src/zilpzalp/web/templates/overview.html`
- Test: `backend/tests/test_routes.py` (existing assertions must keep passing)

- [ ] **Step 1: Replace constants in routes.py**

In `backend/src/zilpzalp/web/routes.py`, replace this block:

```python
# status key → (German label, badge css class)
STATUS_META = {
    "pending": ("wartet", "b-wait"),
    "analyzing": ("Analyse", "b-ana"),
    "ready": ("bereit", "b-ready"),
    "error": ("Fehler", "b-err"),
}

ORIGINAL_LABEL = {"move": "verschieben", "delete": "löschen", "keep": "behalten"}
SUMMARY_LABEL = {"always": "immer", "on_conflict": "bei Konflikt", "never": "nie"}
```

with:

```python
# status key → badge css class (labels live in the i18n catalogs as status.<key>)
STATUS_BADGE = {
    "pending": "b-wait",
    "analyzing": "b-ana",
    "ready": "b-ready",
    "error": "b-err",
}
```

- [ ] **Step 2: Update the Jinja global and label context**

In `backend/src/zilpzalp/web/routes.py`, replace:

```python
templates.env.globals["STATUS_META"] = STATUS_META
```

with:

```python
templates.env.globals["STATUS_BADGE"] = STATUS_BADGE
```

In the `overview` route, remove the now-unused label lookups. Replace this block:

```python
    context.update(
        {
            "recent": _recent(queue),
            "config": config,
            "config_path": str(request.app.state.config_path),
            "original_label": ORIGINAL_LABEL[config.original_handling],
            "summary_label": SUMMARY_LABEL[config.summary_mode],
            "preselected_date": _preselected_date,
        }
    )
```

with:

```python
    context.update(
        {
            "recent": _recent(queue),
            "config": config,
            "config_path": str(request.app.state.config_path),
            "preselected_date": _preselected_date,
        }
    )
```

> Note: `ORIGINAL_LABEL` / `SUMMARY_LABEL` are also referenced in `review_page`, `_summary_response` and the flash logic. Those are migrated in Task 5/6. After this step the module will still reference `ORIGINAL_LABEL` in those spots — that is expected and fixed there. To keep the app importable in the meantime, do NOT delete other usages yet; only the two lines above are changed here.

Actually, to avoid a broken intermediate state, replace every remaining `ORIGINAL_LABEL[config.original_handling]` in `review_page` and `_summary_response` and `_execute_guarded` right now with the catalog lookup. In `review_page` replace:

```python
            "original_label": ORIGINAL_LABEL[config.original_handling],
```

with:

```python
            "original_label": translate("original." + config.original_handling, resolve_language(request)),
```

In `_summary_response`, replace:

```python
        "original_label": ORIGINAL_LABEL[config.original_handling],
```

with (note: `_summary_response` needs `request`, which it already receives):

```python
        "original_label": translate("original." + config.original_handling, resolve_language(request)),
```

The overview template no longer uses `original_label`/`summary_label` (see Step 4), so no catalog lookup is needed there.

- [ ] **Step 3: Update _overview.html**

Replace the full contents of `backend/src/zilpzalp/web/templates/_overview.html` with:

```html
<div id="overview-live">
  <div class="counters">
    {% for kind, var in [('ready','--st-ready'),('analyzing','--st-ana'),('pending','--st-wait'),('error','--st-err')] %}
    <div class="counter" style="--c: var({{ var }})">
      <div class="c-num" style="color: {% if counts[kind] > 0 %}var({{ var }}){% else %}var(--text-3){% endif %}">{{ counts[kind] }}</div>
      <div class="c-label"><span class="c-tag" style="color: var({{ var }})"><span class="dot"></span><span>{{ t('status.' ~ kind) }}</span></span></div>
    </div>
    {% endfor %}
  </div>

  <div class="card card-pad" style="margin-top: var(--gap)">
    <div class="section-head">
      <h3>{{ t('overview.recent') }}</h3>
      <a class="link" href="/queue">{{ t('overview.show_all') }}
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="width:14px;height:14px"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
      </a>
    </div>
    {% if not recent %}
      <div class="empty">
        <div class="e-title">{{ t('overview.empty_title') }}</div>
        <div class="e-sub">{{ t('overview.empty_sub') }}</div>
      </div>
    {% else %}
      <div class="preview-list">
        {% for entry in recent %}
        <div class="preview-item">
          <svg class="ficon" viewBox="0 0 38 44" fill="none" style="width:26px;height:30px"><path d="M7 3h17l8 8v28a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2z" fill="var(--surface-2)" stroke="var(--border-strong)" stroke-width="1.3"/><path d="M24 3v6a2 2 0 0 0 2 2h6" fill="none" stroke="var(--border-strong)" stroke-width="1.3"/><path d="M11 22h16M11 28h16M11 34h10" stroke="var(--text-3)" stroke-width="1.5" stroke-linecap="round"/></svg>
          <div style="min-width:0">
            <div class="pi-name">{{ entry.path.name }}</div>
            <div class="pi-sub">
              {% if entry.status == 'ready' %}
                {{ preselected_date(entry.suggestion) }} · {{ entry.suggestion.sender }} · {{ entry.suggestion.doctype }}
              {% elif entry.status == 'error' %}
                <span style="color: var(--st-err)">{{ entry.error_reason }}</span>
              {% elif entry.status == 'analyzing' %}{{ t('queue.analyzing') }}
              {% else %}{{ t('queue.waiting') }}{% endif %}
            </div>
          </div>
          {% if entry.status == 'ready' %}
            <a class="btn sm primary" href="/review/{{ entry.id }}">{{ t('action.review') }}</a>
          {% else %}
            <span class="badge {{ STATUS_BADGE[entry.status] }}"><span class="dot"></span>{{ t('status.' ~ entry.status) }}</span>
          {% endif %}
        </div>
        {% endfor %}
      </div>
    {% endif %}
  </div>
</div>
```

- [ ] **Step 4: Update overview.html**

Replace the full contents of `backend/src/zilpzalp/web/templates/overview.html` with:

```html
{% extends "base.html" %}
{% block content %}
<div class="view-enter">
  <div class="view-head">
    <h1 class="view-title">{{ t('overview.title') }}</h1>
  </div>
  <div class="dash-cols">
    <div class="dash-stack" hx-get="/partials/overview" hx-trigger="every 2s" hx-swap="innerHTML">
      {% include "_overview.html" %}
    </div>

    <div class="card card-pad">
      <h2 class="card-h">{{ t('overview.operations') }}</h2>
      <div class="info-grid">
        <div class="info-cell"><div class="info-k">{{ t('overview.watchfolder') }}</div><div class="info-v mono">{{ config.paths.watchfolder }}</div></div>
        <div class="info-cell"><div class="info-k">{{ t('overview.config_file') }}</div><div class="info-v mono">{{ config_path }}</div></div>
        <div class="info-cell"><div class="info-k">{{ t('overview.original_handling') }}</div><div class="info-v">{{ t('original.' ~ config.original_handling) }}</div></div>
        <div class="info-cell"><div class="info-k">{{ t('overview.summary') }}</div><div class="info-v">{{ t('summary_mode.' ~ config.summary_mode) }}</div></div>
        <div class="info-cell"><div class="info-k">{{ t('overview.targets') }}</div><div class="info-v">{{ config.targets | length }}</div></div>
        <div class="info-cell"><div class="info-k">{{ t('overview.patterns') }}</div><div class="info-v">{{ config.patterns | length }}</div></div>
        <div class="info-cell span2"><div class="info-k">{{ t('overview.rules') }}</div><div class="info-v">{{ config.rules | length }}</div></div>
      </div>
    </div>
  </div>
</div>
{% endblock %}
```

- [ ] **Step 5: Update _queue_list.html**

Replace the full contents of `backend/src/zilpzalp/web/templates/_queue_list.html` with:

```html
{% if not entries %}
  <div class="empty">
    <div class="e-title">{{ t('queue.empty_title') }}</div>
    <div class="e-sub">{{ t('queue.empty_sub') }}</div>
  </div>
{% else %}
  <div class="qlist">
    {% for entry in entries %}
    <div class="qrow {% if entry.status == 'error' %}dimmed{% endif %}">
      <svg class="ficon" viewBox="0 0 38 44" fill="none"><path d="M7 3h17l8 8v28a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2z" fill="var(--surface-2)" stroke="var(--border-strong)" stroke-width="1.3"/><path d="M24 3v6a2 2 0 0 0 2 2h6" fill="none" stroke="var(--border-strong)" stroke-width="1.3"/><path d="M11 22h16M11 28h16M11 34h10" stroke="var(--text-3)" stroke-width="1.5" stroke-linecap="round"/></svg>
      <div class="qfile">
        <div class="qfile-name">{{ entry.path.name }}</div>
        {% if entry.status == 'ready' %}
          <div class="qfile-prev"><span class="chip mono">{{ preselected_date(entry.suggestion) }}</span><span class="sep">·</span><span class="chip">{{ entry.suggestion.sender }}</span><span class="sep">·</span><span class="chip">{{ entry.suggestion.doctype }}</span></div>
        {% elif entry.status == 'error' %}
          <div class="qfile-prev" style="color: var(--st-err)">{{ entry.error_reason }}</div>
        {% elif entry.status == 'analyzing' %}
          <div class="qfile-prev">{{ t('queue.analyzing') }}</div>
        {% else %}
          <div class="qfile-prev">{{ t('queue.waiting') }}</div>
        {% endif %}
      </div>
      <span class="badge {{ STATUS_BADGE[entry.status] }}"><span class="dot"></span>{{ t('status.' ~ entry.status) }}</span>
      <div style="width:92px;display:flex;justify-content:flex-end">
        {% if entry.status == 'ready' %}
          <a class="btn sm primary" href="/review/{{ entry.id }}">{{ t('action.review') }}
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="width:14px;height:14px"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
          </a>
        {% endif %}
      </div>
    </div>
    {% endfor %}
  </div>
{% endif %}
```

- [ ] **Step 6: Run the route tests**

Run: `python -m pytest tests/test_routes.py -v`
Expected: PASS. The existing German assertions (`"Übersicht"`, `"bereit"`, `"Prüfen"`, `"Betriebsangaben"`, empty-state strings) still hold because German is the default language.

- [ ] **Step 7: Commit**

```bash
git add src/zilpzalp/web/routes.py src/zilpzalp/web/templates/_overview.html src/zilpzalp/web/templates/overview.html src/zilpzalp/web/templates/_queue_list.html
git commit -m "refactor(web): externalize status/label strings to i18n catalogs"
```

---

## Task 4: Restliche Templates externalisieren (queue, review, config, summary, toast)

**Files:**
- Modify: `backend/src/zilpzalp/web/templates/queue.html`
- Modify: `backend/src/zilpzalp/web/templates/review.html`
- Modify: `backend/src/zilpzalp/web/templates/config.html`
- Modify: `backend/src/zilpzalp/web/templates/_summary_modal.html`
- Modify: `backend/src/zilpzalp/web/templates/_toast.html`

- [ ] **Step 1: Update queue.html**

Replace the full contents of `backend/src/zilpzalp/web/templates/queue.html` with:

```html
{% extends "base.html" %}
{% block content %}
<div class="view-enter">
  <div class="view-head">
    <h1 class="view-title">{{ t('queue.title') }}</h1>
    <p class="view-sub">{{ t('queue.subtitle') }}</p>
  </div>
  <div hx-get="/partials/queue" hx-trigger="every 2s" hx-swap="innerHTML">
    {% include "_queue_list.html" %}
  </div>
</div>
{% endblock %}
```

- [ ] **Step 2: Update review.html**

Replace the full contents of `backend/src/zilpzalp/web/templates/review.html` with (note the added `data-msg-*` attributes on the form for Task 6, the `t()` calls, and the date output without `german_date`):

```html
{% extends "base.html" %}
{% block content %}
<div class="view-enter">
  <a class="link back-link" href="/queue">
    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="width:15px;height:15px"><path d="M19 12H5M11 6l-6 6 6 6"/></svg>
    {{ t('review.back') }}
  </a>
  <div class="view-head" style="margin-bottom:22px">
    <h1 class="view-title">{{ t('review.title') }}</h1>
    <p class="view-sub mono" style="font-size:13.5px">{{ entry.path.name }}</p>
  </div>

  <form id="review-form" class="review-grid" data-ext="{{ ext }}"
        data-msg-date="{{ t('review.msg_date_required') }}"
        data-msg-target="{{ t('review.msg_target_required') }}">
    <input type="hidden" name="date_kind" value="candidate" />
    <input type="hidden" name="date_value" data-selected-date="{{ suggestion.date_candidates[preselected_index].normalized if suggestion.date_candidates else '' }}" />

    <div class="dash-stack">
      <div class="card card-pad">
        <h2 class="card-h" style="margin-bottom:6px">{{ t('review.choose_date') }}</h2>
        <p class="muted" style="font-size:13px;margin:0 0 16px">{{ t('review.choose_date_hint') }}</p>
        <div class="date-list">
          {% for c in suggestion.date_candidates %}
          <button type="button" class="date-opt {% if loop.index0 == preselected_index %}sel{% endif %}" data-date="{{ c.normalized }}">
            <span class="date-radio"><span class="date-radio-dot"></span></span>
            <span style="min-width:0">
              <span style="display:flex;align-items:baseline;gap:12px;flex-wrap:wrap">
                <span class="date-iso">{{ c.normalized }}</span>
                {% if c.label %}<span class="date-ctx"><span class="ctx-label">{{ c.label }}</span></span>{% endif %}
              </span>
              {% if c.snippet %}
              <span class="date-snippet" style="display:block">„{{ c.snippet.split(c.raw)[0] }}<mark>{{ c.raw }}</mark>{{ c.raw.join(c.snippet.split(c.raw)[1:]) }}"</span>
              {% endif %}
            </span>
          </button>
          {% endfor %}
        </div>
        <div class="manual-date" style="margin-top:10px">
          <span class="date-radio" style="padding:0"><span class="date-radio-dot"></span></span>
          <div style="flex:1">
            <div style="font-size:13.5px;font-weight:560">{{ t('review.manual_date') }}</div>
            <input type="date" id="manual-date" class="input" style="max-width:200px;margin-top:8px" />
          </div>
        </div>
      </div>

      <div class="card card-pad">
        <h2 class="card-h">{{ t('review.fields') }}</h2>
        <div class="field">
          <label class="field-label">{{ t('review.sender') }}</label>
          <input class="input" name="sender" value="{{ suggestion.sender }}" />
        </div>
        <div style="display:grid;grid-template-columns:1fr 1fr;gap:14px">
          <div class="field">
            <label class="field-label">{{ t('review.doctype') }}</label>
            <input class="input" name="doctype" value="{{ suggestion.doctype }}" />
          </div>
          <div class="field">
            <label class="field-label">{{ t('review.pattern') }}</label>
            <select class="select" name="pattern">
              {% for p in config.patterns %}
              <option value="{{ p.name }}" data-template="{{ p.template }}" {% if p.name == suggestion.pattern_name %}selected{% endif %}>{{ p.name }} — {{ p.template }}</option>
              {% endfor %}
            </select>
          </div>
        </div>
        <div class="field">
          <label class="field-label">{{ t('review.description') }}</label>
          <textarea class="textarea" name="description">{{ suggestion.description }}</textarea>
        </div>
        <div class="field">
          <label class="field-label">{{ t('review.targets') }} <span class="faint" style="font-weight:400">{{ t('review.targets_hint') }}</span></label>
          <div class="folder-grid">
            {% for t_target in config.targets %}
            {% set on = (t_target.path | string) in recommended %}
            <button type="button" class="folder-chip {% if on %}on{% endif %}" data-path="{{ t_target.path }}">
              <span class="fk-box">{% if on %}<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" style="width:12px;height:12px"><path d="M5 12.5l4.5 4.5L19 7"/></svg>{% endif %}</span>
              <span style="min-width:0">
                <span style="display:flex;align-items:center;gap:6px">{{ t_target.name }}</span>
                <span class="fk-path">{{ t_target.path }}</span>
              </span>
            </button>
            <input type="checkbox" name="targets" value="{{ t_target.path }}" hidden {% if on %}checked{% endif %} />
            {% endfor %}
          </div>
        </div>
      </div>
    </div>

    <div class="name-preview">
      <div class="np-box">
        <h2 class="card-h" style="margin-bottom:10px">{{ t('review.final_name') }}</h2>
        <div class="np-final" id="np-final"></div>
        <div class="np-meta">
          <div class="np-meta-row"><span class="k">{{ t('review.meta_pattern') }}</span><span class="v">{{ suggestion.pattern_name or '—' }}</span></div>
          <div class="np-meta-row"><span class="k">{{ t('review.meta_original') }}</span><span class="v">{{ original_label }}</span></div>
        </div>
        <div class="review-actions">
          <a class="btn ghost" href="/queue">{{ t('action.cancel') }}</a>
          <button type="button" class="btn primary" id="confirm-btn" style="flex:1;justify-content:center"
                  hx-post="/documents/{{ entry.id }}/confirm" hx-include="#review-form" hx-target="#modal-root" hx-swap="innerHTML">
            {{ t('action.confirm') }}
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="width:15px;height:15px"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
          </button>
        </div>
        <p class="faint" id="confirm-hint" style="font-size:12px;margin:10px 2px 0;text-align:center"></p>
      </div>
    </div>
  </form>
</div>
{% endblock %}
```

> Note: the loop variable was renamed from `t` to `t_target` because `t` is now the translation function in the context. Using `{% for t in ... %}` would shadow it.

- [ ] **Step 3: Update config.html**

Replace the full contents of `backend/src/zilpzalp/web/templates/config.html` with:

```html
{% extends "base.html" %}
{% block content %}
<div class="view-enter">
  <div class="view-head">
    <h1 class="view-title">{{ t('config.title') }}</h1>
    <p class="view-sub">{{ t('config.subtitle') }}</p>
  </div>

  <form class="config-wrap" method="post" action="/config">
    <textarea class="code-editor" name="text" spellcheck="false">{{ config_text }}</textarea>

    {% if errors %}
    <div class="val-errors">
      <div class="ve-title"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" style="width:16px;height:16px"><path d="M12 3.5L22 20H2L12 3.5z"/><path d="M12 10v4.5M12 17.4v.1"/></svg> {{ t('config.errors_title') }}</div>
      <ul>{% for e in errors %}<li>{{ e }}</li>{% endfor %}</ul>
    </div>
    {% endif %}

    <div class="config-bar">
      {% if saved %}<span class="faint" style="font-size:13px">{{ t('config.saved') }}</span>{% endif %}
      <div class="spacer"></div>
      <button class="btn primary" type="submit">{{ t('config.save') }}</button>
    </div>

    <p class="faint" style="font-size:12.5px;margin-top:4px;line-height:1.6">{{ t('config.footnote') }}</p>
  </form>
</div>
{% endblock %}
```

> Note: `{{ t('config.save') }}` renders `Speichern & prüfen`; Jinja autoescape turns `&` into `&amp;`, matching the original markup.

- [ ] **Step 4: Update _summary_modal.html**

Replace the full contents of `backend/src/zilpzalp/web/templates/_summary_modal.html` with:

```html
<div class="modal-scrim" hx-on:click="document.getElementById('modal-root').innerHTML=''">
  <div class="modal" onclick="event.stopPropagation()">
    <div class="modal-head">
      <h2 class="modal-title">{{ t('summary.title') }}</h2>
      <p class="modal-sub">{{ t('summary.subtitle') }}</p>
    </div>
    <div class="modal-body">
      {% if has_conflict %}
      <div class="conflict-banner">
        <svg class="cb-ic" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" style="width:18px;height:18px"><path d="M12 3.5L22 20H2L12 3.5z"/><path d="M12 10v4.5M12 17.4v.1"/></svg>
        <span><b>{{ t('summary.conflict_strong') }}</b> {{ t('summary.conflict_rest') }}</span>
      </div>
      {% endif %}
      <div class="sum-row"><div class="sum-k">{{ t('summary.source') }}</div><div class="sum-v mono">{{ entry.path.name }}</div></div>
      <div class="sum-row"><div class="sum-k">{{ t('summary.final_name') }}</div><div class="sum-v mono" style="color:var(--text)">{{ filename }}</div></div>
      <div class="sum-row">
        <div class="sum-k">{{ t('summary.destinations') }}</div>
        <div class="sum-v" style="flex:1">
          {% for t_target in selected %}
          {% set conf = (t_target.path | string) in conflict_set %}
          <div class="dest-line {% if conf %}conflict{% endif %}">
            {% if conf %}<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round" style="width:14px;height:14px;flex:none"><path d="M12 3.5L22 20H2L12 3.5z"/><path d="M12 10v4.5M12 17.4v.1"/></svg>{% else %}<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linejoin="round" style="width:14px;height:14px;flex:none;color:var(--text-3)"><path d="M3 6.5A1.5 1.5 0 0 1 4.5 5h4l2 2.5h7A1.5 1.5 0 0 1 19 9v8.5a1.5 1.5 0 0 1-1.5 1.5h-13A1.5 1.5 0 0 1 3 17.5v-11z"/></svg>{% endif %}
            <span style="word-break:break-all">{{ t_target.path }}/{{ filename }}{% if conf %}  {{ t('summary.exists') }}{% endif %}</span>
          </div>
          {% endfor %}
        </div>
      </div>
      <div class="sum-row"><div class="sum-k">{{ t('summary.original') }}</div><div class="sum-v"><b>{{ original_label }}</b></div></div>
    </div>
    <div class="modal-foot">
      <form id="summary-form">
        <input type="hidden" name="date_kind" value="{{ form_values.date_kind }}" />
        <input type="hidden" name="date_value" value="{{ form_values.date_value }}" />
        <input type="hidden" name="sender" value="{{ form_values.sender }}" />
        <input type="hidden" name="doctype" value="{{ form_values.doctype }}" />
        <input type="hidden" name="description" value="{{ form_values.description }}" />
        <input type="hidden" name="pattern" value="{{ form_values.pattern }}" />
        {% for path in form_values.targets %}<input type="hidden" name="targets" value="{{ path }}" />{% endfor %}
      </form>
      <button class="btn ghost" type="button" onclick="document.getElementById('modal-root').innerHTML=''">{{ t('action.cancel') }}</button>
      <button class="btn primary" type="button" {% if has_conflict %}disabled{% endif %}
              hx-post="/documents/{{ entry.id }}/execute" hx-include="#summary-form" hx-target="#modal-root" hx-swap="innerHTML">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" style="width:15px;height:15px"><path d="M4 12.5l5 5L20 6.5"/></svg>
        {{ t('action.execute') }}
      </button>
    </div>
  </div>
</div>
```

> Notes: the original line `wird <b>{{ original_label }}</b>` becomes just `<b>{{ original_label }}</b>` (label-only, language-neutral, per spec). Loop variables renamed `t`→`t_target` and the targets loop var renamed to `path` so they don't shadow the translation function `t`. `_summary_modal.html` is rendered by `_summary_response`, which must include `t`/`lang` in its context — see Task 6, Step 2.

- [ ] **Step 5: Update _toast.html**

In `backend/src/zilpzalp/web/templates/_toast.html`, replace the close button's `aria-label`:

```html
  <button class="t-x" data-toast-close aria-label="{{ t('aria.close') }}"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" style="width:14px;height:14px"><path d="M6 6l12 12M18 6L6 18"/></svg></button>
```

> The `{{ flash }}` text itself is produced server-side already translated (Task 6) and passed through the query string; no change to that line.

- [ ] **Step 6: Run the full route test suite**

Run: `python -m pytest tests/test_routes.py -v`
Expected: PASS. German default keeps existing assertions valid (`"Dokument prüfen"`, `"Zusammenfassung"`, `"Namenskonflikt"`, `"Konfiguration"`, etc.).

- [ ] **Step 7: Commit**

```bash
git add src/zilpzalp/web/templates/queue.html src/zilpzalp/web/templates/review.html src/zilpzalp/web/templates/config.html src/zilpzalp/web/templates/_summary_modal.html src/zilpzalp/web/templates/_toast.html
git commit -m "refactor(web): externalize remaining template strings to i18n"
```

---

## Task 5: Datum — `german_date` entfernen + Renaming-Fix

**Files:**
- Modify: `backend/src/zilpzalp/web/routes.py`
- Test: `backend/tests/test_routes.py`

> Templates already output `{{ c.normalized }}` / `{{ preselected_date(...) }}` directly (Task 3/4). This task removes the now-orphaned filter and fixes `_normalize_date`.

- [ ] **Step 1: Write the failing test**

Append to `backend/tests/test_routes.py`:

```python
def test_candidate_date_uses_config_date_format(client):
    cfg = app.state.config
    cfg.__dict__["summary_mode"] = "never"
    cfg.__dict__["date_format"] = "%d.%m.%Y"  # in-memory override for this test
    entry = _add_ready(client, "rechnung.pdf")

    response = client.post(
        f"/documents/{entry.id}/confirm",
        data=_form(cfg.targets[0].path, date_kind="candidate", date_value="2026-01-15"),
    )

    assert response.status_code == 200
    assert response.headers.get("HX-Redirect", "").startswith("/queue")
    target = Path(cfg.targets[0].path)
    names = [p.name for p in target.iterdir()]
    # Candidate date now formatted via config.date_format (15.01.2026), not raw ISO.
    assert any(name.startswith("15.01.2026") for name in names), names
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_routes.py::test_candidate_date_uses_config_date_format -v`
Expected: FAIL — filename starts with `2026-01-15` (raw ISO), not `15.01.2026`.

- [ ] **Step 3: Fix `_normalize_date`**

In `backend/src/zilpzalp/web/routes.py`, replace:

```python
def _normalize_date(date_kind: str, date_value: str, config: Config) -> str:
    if date_kind == "manual":
        try:
            parsed = datetime.datetime.strptime(date_value, "%Y-%m-%d").date()
        except ValueError:
            return ""
        return parsed.strftime(config.date_format)
    return date_value
```

with:

```python
def _normalize_date(date_value: str, config: Config) -> str:
    # Both candidate and manual dates arrive as ISO (YYYY-MM-DD); the rename
    # format always comes from config.date_format.
    try:
        parsed = datetime.datetime.strptime(date_value, "%Y-%m-%d").date()
    except ValueError:
        return ""
    return parsed.strftime(config.date_format)
```

Update its caller in `_build_request_state`. Replace:

```python
    date = _normalize_date(date_kind, date_value, config)
```

with:

```python
    date = _normalize_date(date_value, config)
```

> `_build_request_state` keeps its `date_kind` parameter (still passed by `confirm`/`execute`); it is simply no longer forwarded to `_normalize_date`. No other call sites change.

- [ ] **Step 4: Remove the orphaned `german_date` filter**

In `backend/src/zilpzalp/web/routes.py`, delete the `_ISO_DATE` regex, the `german_date` function, and its filter registration. Remove these blocks:

```python
_ISO_DATE = re.compile(r"^(\d{4})-(\d{2})-(\d{2})$")


def german_date(value: str | None) -> str:
    if not value:
        return "—"
    m = _ISO_DATE.match(value)
    if not m:
        return value
    return f"{m.group(3)}.{m.group(2)}.{m.group(1)}"


templates.env.filters["german_date"] = german_date
```

Then remove the now-unused `import re` at the top of the file (verify with `grep -n "re\." src/zilpzalp/web/routes.py` returns nothing before deleting).

- [ ] **Step 5: Run tests to verify they pass**

Run: `python -m pytest tests/test_routes.py -v`
Expected: PASS (including the new `test_candidate_date_uses_config_date_format`).

- [ ] **Step 6: Commit**

```bash
git add src/zilpzalp/web/routes.py tests/test_routes.py
git commit -m "fix(web): format rename date via config.date_format for candidates; drop german_date display filter"
```

---

## Task 6: Flash-Meldungen übersetzen + `_summary_response`-Context

**Files:**
- Modify: `backend/src/zilpzalp/web/routes.py`
- Test: `backend/tests/test_routes.py`

- [ ] **Step 1: Write the failing test**

Append to `backend/tests/test_routes.py`:

```python
def test_flash_message_is_localized_to_english(client):
    cfg = app.state.config
    cfg.__dict__["summary_mode"] = "never"
    entry = _add_ready(client, "rechnung.pdf")

    response = client.post(
        f"/documents/{entry.id}/confirm",
        data=_form(cfg.targets[0].path),
        cookies={"lang": "en"},
    )

    assert response.status_code == 200
    redirect = response.headers.get("HX-Redirect", "")
    assert "has+been+filed" in redirect or "has%20been%20filed" in redirect


def test_summary_modal_is_localized_to_english(client):
    cfg = app.state.config
    cfg.__dict__["summary_mode"] = "always"
    entry = _add_ready(client, "rechnung.pdf")

    response = client.post(
        f"/documents/{entry.id}/confirm",
        data=_form(cfg.targets[0].path),
        cookies={"lang": "en"},
    )

    assert response.status_code == 200
    assert "Summary" in response.text
    assert "Execute" in response.text
```

- [ ] **Step 2: Add `t`/`lang` to the summary modal context**

In `backend/src/zilpzalp/web/routes.py`, update `_summary_response` so the modal can translate. Replace its `context` block:

```python
    selected = [t for t in config.targets if t.path in target_paths]
    conflict_set = {str(p) for p in conflicts}
    context = {
        "entry": entry,
        "filename": filename,
        "selected": selected,
        "conflict_set": conflict_set,
        "has_conflict": bool(conflicts),
        "original_label": translate("original." + config.original_handling, resolve_language(request)),
        "form_values": form_values,
    }
    return templates.TemplateResponse(request, "_summary_modal.html", context)
```

with:

```python
    lang = resolve_language(request)
    selected = [target for target in config.targets if target.path in target_paths]
    conflict_set = {str(p) for p in conflicts}
    context = {
        "entry": entry,
        "filename": filename,
        "selected": selected,
        "conflict_set": conflict_set,
        "has_conflict": bool(conflicts),
        "original_label": translate("original." + config.original_handling, lang),
        "form_values": form_values,
        "lang": lang,
        "t": lambda key, **kw: translate(key, lang, **kw),
    }
    return templates.TemplateResponse(request, "_summary_modal.html", context)
```

> The list-comprehension variable was renamed `t`→`target` to avoid clashing with the `t` translation callable.

- [ ] **Step 3: Localize the success flash in `_execute`**

In `backend/src/zilpzalp/web/routes.py`, replace `_execute`:

```python
def _execute(request, entry, filename, target_paths, config):
    queue: Queue = request.app.state.queue
    process(entry.path, filename, target_paths, config)
    queue.remove(entry.path)
    resp = Response(status_code=200)
    resp.headers["HX-Redirect"] = "/queue?flash=" + quote(
        f'„{filename}“ wurde abgelegt.'
    ) + "&kind=ok"
    return resp
```

with:

```python
def _execute(request, entry, filename, target_paths, config):
    queue: Queue = request.app.state.queue
    lang = resolve_language(request)
    process(entry.path, filename, target_paths, config)
    queue.remove(entry.path)
    message = translate("toast.filed", lang, filename=filename)
    resp = Response(status_code=200)
    resp.headers["HX-Redirect"] = "/queue?flash=" + quote(message) + "&kind=ok"
    return resp
```

- [ ] **Step 4: Localize the error flash in `_execute_guarded`**

In `backend/src/zilpzalp/web/routes.py`, replace the `except ProcessorError` branch:

```python
    except ProcessorError as exc:
        return Response(
            status_code=200,
            headers={
                "HX-Redirect": f"/review/{entry.id}?flash="
                + quote(f"Fehler bei der Ablage: {exc}") + "&kind=err"
            },
        )
```

with:

```python
    except ProcessorError as exc:
        message = translate("toast.file_error", resolve_language(request), error=str(exc))
        return Response(
            status_code=200,
            headers={
                "HX-Redirect": f"/review/{entry.id}?flash="
                + quote(message) + "&kind=err"
            },
        )
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `python -m pytest tests/test_routes.py -k "localized" -v`
Expected: PASS (2 tests).

- [ ] **Step 6: Commit**

```bash
git add src/zilpzalp/web/routes.py tests/test_routes.py
git commit -m "feat(web): localize flash messages and summary modal"
```

---

## Task 7: app.js-Hinweise lokalisieren

**Files:**
- Modify: `backend/src/zilpzalp/web/static/app.js`

> The review form already carries `data-msg-date` / `data-msg-target` (Task 4, Step 2). This task makes the JS read them instead of hardcoded German. Filename fallbacks (`"Unbekannt"`, `"Dokument"` in `buildName`) stay as-is — they feed the actual filename, which must remain language-independent per spec.

- [ ] **Step 1: Read the hint strings from the form**

In `backend/src/zilpzalp/web/static/app.js`, inside `initReview`, after the line:

```javascript
    var ext = form.getAttribute("data-ext") || ".pdf";
```

add:

```javascript
    var msgDate = form.getAttribute("data-msg-date") || "";
    var msgTarget = form.getAttribute("data-msg-target") || "";
```

- [ ] **Step 2: Use them in the hint logic**

In `backend/src/zilpzalp/web/static/app.js`, replace:

```javascript
      if (hint) {
        hint.textContent = !date ? "Bitte ein Datum wählen."
          : targetCount() === 0 ? "Mindestens einen Zielordner wählen." : "";
      }
```

with:

```javascript
      if (hint) {
        hint.textContent = !date ? msgDate
          : targetCount() === 0 ? msgTarget : "";
      }
```

- [ ] **Step 3: Manually verify in the browser (no automated JS test in this repo)**

Run the app and confirm the hint text matches the selected language:

```bash
ZILPZALP_CONFIG=../demo/config.yaml python -m uvicorn zilpzalp.main:app --port 8000
```

Open `http://localhost:8000/lang/en?next=/`, then a review page, clear the date — the hint reads "Please choose a date." Switch to `de` and confirm German. Stop the server (Ctrl-C).

> If a review document isn't readily available in the demo, this step can be skipped with a note; there is no JS unit-test harness in this project.

- [ ] **Step 4: Commit**

```bash
git add src/zilpzalp/web/static/app.js
git commit -m "feat(web): localize review hint messages in app.js"
```

---

## Task 8: Gesamtverifikation

**Files:** none (verification only)

- [ ] **Step 1: Run the full test suite**

Run: `python -m pytest -v`
Expected: PASS — all tests green (existing + new i18n/route tests).

- [ ] **Step 2: Grep for leftover hardcoded German UI strings**

Run:

```bash
grep -rnE "Übersicht|Warteschlange|Konfiguration|Prüfen|Bestätigen|Ausführen|Abbrechen|Zusammenfassung|wartet|bereit" src/zilpzalp/web/templates/
```

Expected: no matches in templates (all such text now comes via `t(...)`). Matches inside `locales/*.json` are expected and fine.

- [ ] **Step 3: Smoke-check both languages render**

Run the app (`ZILPZALP_CONFIG=../demo/config.yaml python -m uvicorn zilpzalp.main:app --port 8000`), open `/` (German), click the `EN` switch, confirm nav reads Overview / Queue / Configuration and the cookie persists across a reload. Stop the server.

- [ ] **Step 4: Final commit (if any verification fixups were needed)**

```bash
git add -A
git commit -m "test(web): verify DE/EN i18n end to end"
```

> After the plan is fully executed and verified, update `docs/backlog.md` row #3 Status to ✅ and fill the `Commit` column with the final (merge) commit SHA, per the backlog maintenance rule.

---

## Self-Review

**Spec coverage:**
- DE + EN full catalogs → Task 1 (de.json/en.json) + completeness test. ✓
- Switcher + cookie + Accept-Language default → Task 1 (`resolve_language`) + Task 2 (`/lang`, switcher). ✓
- Mechanism = lightweight JSON + `t()` → Task 1 (`translate`) + Task 2 (context injection). ✓
- Date display = ISO, no locale → Task 3/4 (`{{ c.normalized }}`) + Task 5 (filter removal). ✓
- Renaming fix via config.date_format → Task 5 (`_normalize_date`) + test. ✓
- Externalize routes constants + flash → Task 3 (status/labels) + Task 6 (flash, summary modal). ✓
- Out of scope (config.yaml content, Babel, plurals) → respected; no tasks add them. ✓

**Placeholder scan:** No TBD/TODO; every code/edit step shows full content. ✓

**Type/name consistency:** `STATUS_BADGE` (Task 3) used in `_overview.html`/`_queue_list.html` (Task 3). `translate`/`resolve_language`/`SUPPORTED` imported once (Task 2) and reused (Tasks 3/5/6). `_normalize_date(date_value, config)` new signature (Task 5) matches its only caller `_build_request_state` (Task 5). `t`→`t_target`/`target`/`path` rename applied wherever a loop/comprehension would shadow the `t` context callable (Tasks 4, 6). ✓
